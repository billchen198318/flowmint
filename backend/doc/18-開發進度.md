# 18 開發進度

最後更新：2026-08-26
目前開發重點：少數平台擴充能力補齊，以及正式流程 E2E／UAT

> 本文件依日期保留歷史開發紀錄。較早段落中的「尚未實作」只代表當時狀態；若與後續段落衝突，以日期較新的段落及目前程式碼為準。

## 強制開發原則

- FlowMint 前後端程式必須以 `C:\home\qifu4` 現有程式為實作範本。
- 開發前必須閱讀 `15-QIFU4前端實作規範.md`、`16-QIFU4後端持久層規範.md`、`17-QIFU4帳號與Role邊界.md`。
- 不得自行發明查詢、Grid、Toolbar、toast、欄位驗證、Controller 結果封裝或持久層流程。
- Query／Create／Edit 頁面應先選定相近的 QIFU4 範例，再替換為 FlowMint 業務欄位。
- 後端分頁查詢必須使用 QIFU4 的 `setQueryResponseJsonResult(...)` 封裝回應。
- 表單驗證失敗時，前端必須同時套用後端 `checkFields`，並以 `toast.warning(escapeQifuHtmlMsg(response.data.message))` 顯示訊息。

## 已完成

### BPMN framework

- `backend/app/pom.xml` 已加入 Flowable BPMN framework 相關依賴，版本為 `8.0.0`。

### FM_PROG001D0001 後端

已建立 Tenant／Tenant Account 垂直切片：

- Entity：`FmTenant`、`FmTenantAccount`
- Mapper interface 與 MyBatis XML
- Service interface 與 implementation
- Command／View DTO
- `IFmTenantLogicService`、`FmTenantLogicServiceImpl`
- `FMPROG001D0001Controller`
- MyBatis mapper scan 已包含 `org.qifu.fm.mapper`

API 包含：

- `POST /api/FM_PROG001D0001/findPage`
- `POST /api/FM_PROG001D0001/save`
- `POST /api/FM_PROG001D0001/load`
- `POST /api/FM_PROG001D0001/update`
- `POST /api/FM_PROG001D0001/deactivate`
- `POST /api/FM_PROG001D0001/account/save`
- `POST /api/FM_PROG001D0001/account/update`
- `POST /api/FM_PROG001D0001/account/reset-password`
- `POST /api/FM_PROG001D0001/account/deactivate`
- `POST /api/FM_PROG001D0001/account/activate`

Tenant ID 與 Tenant 代碼都是建立後不可變更的穩定識別資料；Edit 頁面鎖定兩個欄位，後端 Update 與 Mapper 亦不更新 TENANT_CODE。

### FM_PROG001D0001 前端

目錄：`frontend-v-nx/pages/fm_prog001d0001/`

已建立：

- `index.vue`
- `create.vue`
- `edit/[id].vue`
- `components/TenantForm.vue`
- `QueryPageStore.ts`
- `config.ts`

目前介面要求：

- Create／Edit 表單底部保留「儲存／清除」等操作按鈕。
- 表單底部不放「返回」按鈕；Toolbar 返回功能可保留。
- Vue 排版使用 Prettier。
- 表單儲存驗證依 QIFU4 後端 `checkFields` 與 toast 流程處理。

### 程式與目錄 SQL

- 已建立 `backend/doc/FM_PROG001D0001-register.sql`，供 `tb_sys_prog` 與程式目錄註冊使用。

## 2026-07-30 問題修正紀錄

### 必填欄位只有紅框、沒有 toast

原因：前端曾自行加入驗證並提前中止，未依 QIFU4 使用後端回傳訊息。  
修正：Create／Edit 儲存改為接收 `response.data.checkFields`，失敗時呼叫：

```ts
checkFields.value = response.data?.checkFields || {};
toast.warning(escapeQifuHtmlMsg(response.data.message));
```

### SQL 查到資料但 Grid 沒有顯示

後端 log 已確認：

- `fm_tenant` count 為 `1`
- 分頁 SQL 確實查到 Tenant `A01`
- HTTP 回應為 `200 OK`

真正原因：`FMPROG001D0001Controller.findPage()` 手動複製 Service 的 `QueryResult`。Service 內部結果的 `success` 預設為 `N`，造成前端進入失敗分支並保持空 Grid。

已改為 QIFU4 標準結果封裝：

```java
this.setQueryResponseJsonResult(viewResult, result, searchBody.getPageOf());
```

同時前端查詢頁已補上：

- 按「查詢」先回第 1 頁。
- 查詢前清空舊列表。
- QIFU4 成功旗標判斷。
- 空回應、查詢錯誤與分頁總筆數處理。

## 驗證狀態

- `mvn -pl app -am -DskipTests compile`：通過。
- `npm run build`：通過。
- 完整 Maven tests：既有 `MqttIntegrationTest` 因缺少 `MqttConfigProperties` 失敗，與本程式無關。
- Grid 查詢修正：程式與編譯已完成；尚需重新啟動後端後，以瀏覽器確認 Tenant `A01` 顯示於 Grid。未完成此步前不得標記為 UI 驗收完成。

## 下一步

1. 重新啟動後端。
2. 進入 `FM_PROG001D0001Q`，使用空條件查詢。
3. 確認 Grid 顯示 `A01／台灣區`，總筆數為 1，編輯按鈕可進入 Edit 頁。
4. 依 QIFU4 範例逐頁重新檢查 Query／Create／Edit 的 Toolbar、Grid、toast、loading、例外處理與 store 行為。
5. 完成實際畫面驗收後，更新本文件的驗證狀態。

## 工作區注意事項

以下為使用者既有或其他範圍的變更，不可任意覆蓋：

- `backend/doc/flowmint.sql`
- `.sonarlint/`
## 2026-07-31 FM_PROG002D0001 開發

### FM_PROG002D0001 員工資料管理

已建立員工基本資料 Q／A／E 垂直切片：

- Entity：`FmEmployee`
- Mapper interface 與 MyBatis XML
- Service interface 與 implementation
- Command／View DTO
- `IFmEmployeeLogicService`、`FmEmployeeLogicServiceImpl`
- `FMPROG002D0001Controller`
- 前端 Query／Create／Edit 與共用 `EmployeeForm`
- `FM_PROG002D0001-register.sql`

API 包含：

- `POST /api/FM_PROG002D0001/findPage`
- `POST /api/FM_PROG002D0001/save`
- `POST /api/FM_PROG002D0001/load`
- `POST /api/FM_PROG002D0001/update`
- `POST /api/FM_PROG002D0001/deactivate`
- `POST /api/FM_PROG002D0001/tenant-options`
- `POST /api/FM_PROG002D0001/account-options`

員工編號是建立後不可變更的業務識別碼；Edit 頁面鎖定該欄位，後端 Update 與 Mapper 亦不更新 `EMPLOYEE_NO`。

目前已完成員工基本資料與員工任職整合。Edit 頁面可維護主要／兼任部門、Tenant 共用職稱、主管來源、指定主管任職、有效期間及啟停狀態；下拉選項顯示「代碼／名稱」，主管選項顯示「員工編號／姓名／部門」。一般情境以 `ORG_HEAD` 推算本部門主管，特殊例外才使用 `EXPLICIT`。

員工任職後端已建立：

- Entity：`FmEmployeeOrgAssignment`
- Mapper interface 與符合明確欄位規範的 MyBatis XML
- Service interface 與 implementation
- Command／View DTO
- `IFmEmployeeOrgAssignmentLogicService`、`FmEmployeeOrgAssignmentLogicServiceImpl`
- 員工 Edit 的 `EmployeeAssignmentSection`

新增 API：

- `POST /api/FM_PROG002D0001/assignment/list`
- `POST /api/FM_PROG002D0001/assignment/save`
- `POST /api/FM_PROG002D0001/assignment/deactivate`
- `POST /api/FM_PROG002D0001/assignment/org-unit-options`
- `POST /api/FM_PROG002D0001/assignment/title-options`
- `POST /api/FM_PROG002D0001/assignment/manager-options`

驗證包含 Tenant 邊界、有效部門／職稱、主要任職唯一、有效期間、指定主管任職有效性與不可指定自己。此項是 `FM_PROG002D0005` 部門主管配置檢查員工是否具有所選部門有效任職的前置功能；`fm_org_unit_head` 依既有表結構保存 `EMPLOYEE_ID`。

## 2026-07-31 FM_PROG002D0003 組織簽核層級

簽核層級方案代碼是建立後不可變更的業務識別碼；Edit 頁面鎖定該欄位，後端 Update 與 Mapper 亦不更新 `SCHEME_CODE`。

已建立方案主檔、層級明細、Q／A／E、後端整合交易與程式註冊 SQL。驗證包含層級代碼／順序不可重複，且有效層級必須且只能有一個最高層級。


## 2026-07-31 FM_PROG002D0002 部門資料與組織樹

部門代碼是建立後不可變更的業務識別碼；Edit 頁面鎖定該欄位，後端 Update 與 Mapper 亦不更新 `UNIT_CODE`。

已建立部門穩定主檔與版本檔、Q／A／E、組織樹顯示、移動 Preview 與子樹交易更新。後端負責計算 TREE_DEPTH 與 PATH，並檢查 Tenant、單一根節點、父子循環及 CURRENT_VERSION_NO optimistic version。前端拖放只先預覽，確認後才正式移動。

職稱代碼是建立後不可變更的業務識別碼；FM_PROG002D0004 Edit 頁面鎖定該欄位，後端 Update 與 Mapper 亦不更新 `TITLE_CODE`。

## 2026-08-02 FM_PROG002D0005 部門主管配置

已建立 `fm_org_unit_head` 的 Entity、MyBatis Mapper、Service、Logic、Controller，以及獨立 Q／A／E 前端頁面與程式註冊 SQL。功能維護主要主管 `HEAD`、副主管 `DEPUTY_HEAD` 與代理主管 `ACTING_HEAD`。

主管候選人只列出所選部門內具有有效員工任職的人員；部門與人員下拉皆顯示代碼／名稱。後端驗證 Tenant 邊界、部門有效性、員工部門任職、有效期間，以及同部門主要主管有效期間不得重疊。註冊 SQL 僅建立程式資料，目前不配置角色權限。

Query、Create、Edit Toolbar 已加入面向使用者的完整操作說明，解釋三種主管類型、任職前置條件、主要主管唯一性及代理期間用途。

## 2026-08-02 FM_PROG002D0006 部門職務與擔任人（選配）

已建立部門職務主檔與職務擔任人的完整 Q／A／E 垂直切片。此功能明確標示為選配：一般主管簽核使用 FM_PROG002D0005，跨部門固定成員使用簽核群組；只有流程需依部門尋找固定專責人員時才建立部門職務。

職務代碼是建立後不可變更的業務識別碼；Edit 頁面鎖定該欄位，後端 Update 與 Mapper 亦不更新 `DUTY_CODE`。

功能包含職務用途（簽核、會簽／審查、通知）、有效期間、啟停狀態，以及從同部門有效員工任職中新增、編輯與停用擔任人。後端驗證 Tenant 與部門邊界、員工任職、期間及單一有效主要擔任人。程式註冊 SQL 不配置角色權限。

## 2026-08-02 FM_PROG003D0001 簽核群組

已完成簽核群組與群組成員的 Q／A／E 垂直切片，包含 Entity、MyBatis Mapper、Service、Logic、Controller、前端頁面與程式註冊 SQL。群組支援候選人、全員會簽及依優先順序簽核三種派送方式；成員可設定優先順序與有效期間。

後端驗證 Tenant 邊界、群組代碼唯一、員工啟用狀態、成員重複、有效期間與派送方式。前端下拉顯示員工編號與姓名，Query 提供查詢及清除，Create 頁面提供儲存及清除，Edit 頁面提供儲存、重新載入及停用，並可在同頁新增、編輯及停用群組成員。Toolbar 說明已使用一般使用者可理解的文字解釋適用情境與三種派送方式；程式註冊 SQL 僅建立程式資料，目前不配置角色權限。
## 2026-08-02 FM_PROG003D0002 工作代理

完成 `fm_workflow_delegation` 的 Entity、MyBatis Mapper、Service、Logic、Controller，以及 Query／Create／Edit Vue 頁面與程式註冊 SQL。功能可依 Tenant 設定被代理人、代理人、全部流程／指定流程／指定簽核群組、是否允許再次轉代理、有效期間、狀態與原因。

新增與編輯均檢查同 Tenant 有效帳號、禁止自己代理自己、結束時間晚於開始時間、指定範圍必填，以及同一被代理人與相同範圍的啟用期間不可重疊。Edit 固定 Tenant 與被代理人，頁面下方依規範提供儲存、重新載入與停用按鈕；Create 提供儲存與清除按鈕；Query 提供查詢與清除按鈕。程式僅註冊於 `tb_sys_prog`，目前不配置角色權限。
## 2026-08-02 FM_PROG004D0001 流程設計與版本

完成流程主檔與 BPMN 版本的 Entity、MyBatis Mapper、Service、Logic、Controller、Query／Create／Edit Vue 頁面及程式註冊 SQL。前端使用 bpmn-js 視覺化設計器；後端支援草稿儲存、建立新版本、Flowable parse／deployment、SHA-256 與發布版本鎖定。Tenant 與流程代碼在 Edit 固定，頁面下方依規範提供儲存、重新載入與停用按鈕；目前不配置角色權限。

已補上選取 User Task 後的節點屬性面板、同 Tenant 已發布表單單選、`fm_task_form_rule` 儲存、新版本設定複製，以及發布前的缺漏／失效表單檢查。一個 User Task 僅允許綁定一張 Form；資料庫唯一條件為 Tenant、流程定義、流程版本與 `TASK_DEF_KEY`。

設計決策調整：`FM_PROG004D0001` 將作為唯一的完整流程設計入口；Task Policy、Assignment Rule／Resolver、Resolver Preview 與發布檢查全部在選取 User Task 後的右側屬性面板內完成。`FM_PROG004D0002` 與 `FM_PROG004D0003` 不再建立獨立使用者頁面、選單或 `tb_sys_prog`，原訂能力併入 `FM_PROG004D0001`。目前 Form 綁定已完成；Task Policy、Resolver、Preview 與整合發布檢查仍待在 `FM_PROG004D0001` 內接續實作。

## 2026-08-03 FM_PROG004D0001 Task Policy

已在 User Task 右側屬性面板加入派送方式、自簽政策、重複簽核政策、意見必填，以及駁回、退回、轉派與加簽開關。Task Policy 與 BPMN、表單綁定由同一個草稿儲存 API 寫入；建立新版本時會複製最新版政策，發布前會檢查每個 User Task 都有政策。

後端已建立 `FmTaskPolicy` 的 Entity、完整 QIFU4 Mapper、Service 與流程 Logic 整合。後端 compile、MyBatis XML parse、Vue Prettier 與前端 production build 已通過。

Assignment Rule／Resolver、共用 Resolver Domain Service、Resolver Preview 與最終整合發布檢查仍待接續實作；目前流程即使通過 Form 與 Task Policy 檢查，仍會阻止發布並提示先完成 Resolver。

## 2026-08-09 FM_PROG004D0001 Assignment Rule 第一階段

已完成 `fm_task_assignment_rule` 的 Entity、Mapper、Service 與流程版本整合；草稿可儲存 User Task 簽核人規則，讀取流程版本時會回傳規則，新建版本也會複製上一版規則。User Task 右側屬性面板已加入 13 種 Resolver 類型、規則參數、結果上限與啟用狀態設定，新 User Task 預設採「申請人的直屬主管」。發布檢核已改為要求每個 User Task 同時具備 Task Policy 與至少一筆啟用的 Assignment Rule，不再無條件阻止發布。

後端 Maven compile 與前端 Nuxt production build 均通過。下一階段仍須完成共用 Resolver Domain Service、依實際申請人／組織解析簽核人的 Preview，以及把目前規則參數 JSON 欄位改為各 Resolver 專用的結構化操作元件。

## 2026-08-09 FM_PROG004D0001 Resolver Preview 第一階段

已建立共用 `FmAssignmentResolverService` 與 `/resolver-preview` API，流程設計頁可輸入測試申請人帳號，依已儲存的 Assignment Rule 顯示各 User Task 的解析狀態、簽核人與失敗原因。第一階段已接通 `DIRECT_MANAGER` 與 `INITIATOR_ORG_HEAD`，會檢查啟用員工、主要部門配置、直屬主管配置及單位主管；其他 Resolver 類型明確回傳 `UNSUPPORTED`，不會產生假結果。另補上員工部門配置 Mapper 對 `EMPLOYEE_ORG_ASSIGNMENT_ID` 的查詢條件。

後端 Maven compile、前端 Nuxt production build 與 `git diff --check` 均通過，未異動資料庫。後續須繼續完成其餘 Resolver 類型、有效日期判斷、Fallback、最大結果數及各 Resolver 專用的結構化參數元件。

## 2026-08-09 FM_PROG004D0001 主管鏈 Resolver

共用 Resolver Service 已擴充 `PARENT_ORG_HEAD`、`ROOT_ORG_HEAD`、`MANAGER_CHAIN` 與 `LEVEL_HEAD_CHAIN`。解析時依目前啟用的組織版本向上尋找父單位，依主管優先序選取單位主管，逐級結果依員工去重並遵守 `MAX_RESULTS`；組織與直屬主管鏈均加入 visited-set，避免錯誤資料造成無限循環。後端 Maven compile 與 `git diff --check` 通過，並以現有 MariaDB 員工配置進行唯讀核對，未異動資料庫。

## 2026-08-09 FM_PROG004D0001 指定帳號與簽核群組 Resolver

已完成 `FIXED_ACCOUNT` 與 `APPROVAL_GROUP` 的實際解析。指定帳號保存 `accounts` 陣列並依 Tenant 啟用員工解析；簽核群組保存不可變的 `approvalGroupId`，執行時重新讀取啟用群組與啟用成員，依成員優先序回傳並遵守 `MAX_RESULTS`。員工、部門配置、單位主管、組織版本與群組成員均加入生效起迄日判斷。

流程設計器已新增啟用員工與簽核群組選項 API；`FIXED_ACCOUNT` 使用帳號複選，`APPROVAL_GROUP` 使用群組單選，不再要求使用者手寫 JSON。後端 Maven compile、前端 Nuxt production build 與 `git diff --check` 均通過，未異動資料庫。

## 2026-08-09 FM_PROG004D0001 簽核層級 Resolver

已完成 `NEXT_HIGHER_LEVEL_HEAD` 與 `TARGET_LEVEL_HEAD`。Resolver 由員工主要配置的 `TITLE_ID` 取得職稱及 `APPROVAL_LEVEL_ID`，再沿直屬主管鏈比較 `LEVEL_ORDER`；數字較小代表較高層級。指定層級保存不可變的 `approvalLevelId`，流程設計器提供同 Tenant 啟用層級選單，不需手寫 JSON。主管鏈具循環防護，職稱與層級也套用狀態及生效期間檢查。

另補上 `FmOrgTitleMapper` 的 `TITLE_ID` 及 `FmOrgApprovalLevelMapper` 的 `APPROVAL_LEVEL_ID` 精確查詢條件。後端 Maven compile、前端 Nuxt production build 與 `git diff --check` 均通過，未異動資料庫。

## 2026-08-09 FM_PROG004D0001 組織職稱與職務 Resolver

已完成 `ORG_TITLE` 與 `ORG_DUTY`。`ORG_TITLE` 保存 `titleId`，解析申請人主要部門內具有指定職稱的啟用任職者；`ORG_DUTY` 保存 `dutyId`，先確認職務屬於申請人主要部門，再由有效的員工職務配置解析任職者。兩者均依員工去重、遵守 `MAX_RESULTS`，並套用 Tenant、狀態及生效期間限制。

流程設計器已提供職稱與職務選單，不需手寫 JSON；目前資料庫的組織職務與員工職務皆為 0 筆，因此畫面會顯示尚未建立職務資料，不擅自產生資料。另補上員工部門配置的 `TITLE_ID` 與組織職務的 `DUTY_ID` 精確查詢條件。後端 Maven compile 與前端 Nuxt production build 通過，未異動資料庫。

## 2026-08-09 FM_PROG004D0001 核決權限條件核心

`APPROVAL_AUTHORITY` Resolver 的前置管理程式與條件格式原先尚未實作，且 `fm_approval_authority`、`fm_approval_authority_rule` 目前皆為 0 筆。為避免 Resolver 自行猜測條件格式，已先正式定義 `CONDITION_CONFIG` 受控 JSON schema：`match` 僅允許 `ALL`、`ANY`；運算子僅允許 `EQ`、`NE`、`GT`、`GTE`、`LT`、`LTE`、`IN`、`NOT_IN`，禁止 JavaScript、SQL 及任意可執行運算式。

已新增 `FmApprovalAuthorityConditionEvaluator`，支援點分隔巢狀欄位、數值、字串、布林及集合比較。三項單元測試涵蓋 ALL、ANY、數值、分類集合與 NOT_IN，全部通過。核決權限不再作為獨立的 `FM_PROG004D0004` 開發，而是併入 `FM_PROG004D0001` 的 User Task 簽核人設定；後續完成 Authority／Rule CRUD、結構化條件 UI，再回接 `APPROVAL_AUTHORITY` Resolver 的目標解析。

## 2026-08-09 FM_PROG004D0001 核決權限 API 契約與條件元件

已定義核決權限主檔與條件規則的 Command／View API 契約，涵蓋適用 Tenant、流程、表單、生效期間、規則順序、條件、目標類型、目標識別、停止條件與狀態。前端新增可重用的 `ApprovalAuthorityConditionEditor`，以表單欄位、白名單運算子和比較值建立 ALL／ANY 條件，可新增、刪除條件並正確產生集合、數值、布林或字串值，不向使用者顯示 JSON 編輯器。

後端 Maven compile、前端 Nuxt production build 與 `git diff --check` 均通過。下一階段接續 Entity／Mapper／Service 與內嵌 CRUD API；本功能不註冊獨立選單，並且本階段未異動資料庫。

## 2026-08-03 FM_PROG005D0001 表單設計與版本

已建立 `fm_form_def` 與 `fm_form_version` 的 Entity、完整 QIFU4 Mapper、Service、Logic、Controller，以及 Query／Create／Edit 前端頁面與程式註冊 SQL。

建立表單時會自動產生第 1 版草稿；一般使用者透過內嵌 Form.io Builder，從工具箱拖拉欄位至設計畫布並以屬性視窗設定內容，不需自行撰寫 JSON。系統將 Form.io Schema 儲存在版本內容，並僅在進階區域提供唯讀內容供除錯；舊版 JSON Schema 會在前端載入時轉換為 Form.io 基本元件。Builder 開放 Basic、Advanced、Data 與 Layout 元件群組，允許內部表單設計者使用 URL Data Source、Custom Default、Calculated Value、Conditional 與 Logic 等 Form.io 客製功能；後端保留 JSON 結構驗證，但允許保存自訂 JavaScript 與 Logic。發布時正規化 JSON、計算 SHA-256 並鎖定版本。已發布表單如需調整，必須建立新草稿版本；新版本會複製最新版內容，發布後上一個 Published 版本轉為 Retired。

`FM_PROG004D0001` 的表單選項查詢會讀取同 Tenant、未停用且版本狀態為 Published 的表單，因此完成資料庫程式註冊並發布至少一張表單後，即可在 User Task 屬性面板選取。

## 2026-08-04 FM_PROG006D0001 DataSource Pool 管理

已建立 DataSource Pool 的 Entity、MyBatis Mapper、Service、Logic、Controller、Q／A／E 前端頁面、建表 SQL 與程式註冊 SQL。第一版支援 MariaDB、Oracle 與 MSSQL 三種 JDBC Driver；Driver Class 由後端白名單決定，不接受前端任意指定。

功能包含 Tenant 範圍、連線池代碼與名稱、JDBC URL、資料庫帳號、HikariCP 連線數與 Timeout、驗證 SQL、啟停狀態及連線測試。資料庫密碼以 AES-GCM 加密保存，Load／Query API 僅回傳是否已設定密碼，不回傳密碼內容；編輯時密碼留空代表沿用既有密碼。連線測試使用單一短生命週期 Hikari Pool，測試完成後立即關閉。

## 2026-08-04 FM_PROG006D0002 Data Action 設計 Phase 1

已建立 Data Action、Action Version 與 SQL Step 的 Entity、MyBatis Mapper、Service、Logic、Controller、Q／A／E 前端頁面、建表 SQL與程式註冊 SQL。一般主檔遵守 QIFU4 `IBaseMapper` 八個標準 statement 與 Service 分層；外部業務資料庫的動態 SQL 僅由獨立 Executor 使用 `NamedParameterJdbcTemplate` 執行。

第一階段包含草稿與發布版本、發布內容 SHA-256、發布後鎖定、編輯已發布 Action 時建立下一版草稿、多個依序 SQL Step、`SELECT_ONE`、`SELECT_LIST`、`INSERT`、`UPDATE`、`DELETE`、Named Parameter、單一 DataSource Transaction、Preview 強制 Rollback、最大回傳列數、Query Timeout 與統一結果組裝。正式 Execute API 只允許已發布版本，並驗證登入帳號的 Tenant membership；`tenantId` 與 `loginAccount` 等 Server Context 不接受 Request Body 覆蓋。

SQL 發布檢查拒絕分號、多語句、SQL comment、`${...}` 字串替換、未定義 Named Parameter，以及 QUERY Action 內的異動 Statement。此段記錄 Phase 1 當時狀態：當時 `FOR_EACH`／JDBC Batch、Generated Key、前一步結果引用與 Execution Audit 尚未實作；後續完成情況見下方 2026-08-25 Phase 2 與 Execution Audit 紀錄。

### 2026-08-25 Data Action 進階能力程式碼複核

本次重新依 Executor、SQL 發布驗證器、前端 Step 編輯畫面及相關 Entity／DTO 核對。確認目前可執行能力為：多 Step 依序執行、五種 Statement Type、Named Parameter、單一 DataSource Transaction、Preview Rollback、Query Timeout、`maxRows`、預期異動筆數與結果組裝。

確認尚未完成：`FOR_EACH`／JDBC Batch、Insert Generated Key、把前一步結果注入後一步參數、Continue Condition、Execution Audit 持久化、統一分頁／Streaming 與 Rate Limit。`executionMode`、`arrayPath`、`continueCondition` 雖已存在於部分資料表、Entity 或 DTO，仍只是預留欄位；發布驗證明確只接受 `ONCE`，前端也只提供 `ONCE`。Executor 回傳 `executionId` 與各 Step Response，不代表已有 Execution Audit 或 Step Context 引用能力。

### 2026-08-25 Data Action Phase 2 執行核心

已接入 `FOR_EACH`／JDBC Batch、Insert Generated Key、`${steps.<resultKey>...}` 前一步結果引用、`${item...}` 項目參數，以及 Continue Condition 的存在判斷、完整比較運算子與 `&&`／`||` 邏輯組合。`FOR_EACH` 一般異動使用 JDBC Batch；需要 Generated Key 時採逐筆 INSERT 並回傳 `generatedKeys`，避免批次鍵值在不同 JDBC Driver 間的不一致。

後端發布驗證與 FM_PROG006D0002 Step 編輯畫面已同步開放上述設定。Maven reactor compile、Data Action 相關 10 項測試及 Nuxt production build 通過；MariaDB、Oracle、MSSQL 的實際 Driver 整合驗收尚未完成。此段完成時 Execution Audit 尚未實作，後續程式與 migration 完成情況見下一節；統一分頁／Streaming 與 Rate Limit 仍未實作。

### 2026-08-25 Data Action Execution Audit

已新增 `fm_data_action_execution_audit` migration 與主 schema 定義，Executor 會在成功、失敗及 Preview Rollback 後寫入 FlowMint 主資料庫，保存 Execution ID、Tenant、Action／版本、登入帳號、狀態、Step 數、參數數、起訖時間、耗時與受限長度的錯誤摘要。稽核不保存完整 Request／Response、密碼或 Token；稽核寫入異常只記錄 server error，不會取代原 Data Action 的業務結果或錯誤。

Data Action 相關測試目前共 12 項通過。2026-08-25 取得使用者明確同意後，已執行 `FM_PROG006D0002-execution-audit-schema.sql`：本機 `flowmint` MariaDB 已建立 `fm_data_action_execution_audit`，回查為 InnoDB／`utf8mb4_unicode_ci`、16 個欄位、Execution ID 唯一索引及兩組 Tenant 查詢索引，初始資料 0 筆，未異動任何既有資料。

Execution Audit 查詢能力亦已完成：新增受 `FM_PROG006D0002Q` 權限及 Tenant membership 雙重保護的分頁 API，可依 Execution ID、Action Code、狀態與登入帳號精確查詢；前端新增「執行紀錄」入口與清單頁，顯示版本、Preview、耗時及錯誤摘要，不顯示 Request／Response。後端 compile 與 Nuxt production build 通過。

### 2026-08-25 Data Action Rate Limit 第一版

已在 Executor 執行 SQL 前加入 `Tenant + Action + Login Account` 每分鐘呼叫限制，Preview 與正式 Execute 共用相同防線。計數直接使用 FlowMint 主資料庫的 Execution Audit，因此多節點部署共享計數；預設每分鐘 60 次，可用 `FM_DATA_ACTION_RATE_LIMIT_PER_MINUTE` 調整且最低為 1。超限請求不會連接或執行外部業務 SQL。Data Action 相關測試目前共 13 項通過；個別 Action 自訂門檻與更嚴格的原子併發配額仍屬後續增強。

### 2026-08-25 Data Action SELECT_LIST 統一分頁

`SELECT_LIST` Request 同時提供正整數 `page` 與 `pageSize` 時，Executor 會執行受控 Count Query 並回傳 `items`、`page`、`pageSize`、`total`、`totalPages`；未提供時維持既有 LIST Response，相容現有 Action。`pageSize` 不得超過 Step `maxRows`，頁碼及 offset 亦有溢位保護。Count Query 會移除最外層 `ORDER BY`，保留子查詢與字串中的內容。另修正後端 Result Mode 白名單遺漏 `GENERATED_KEY`，避免前端可選但儲存遭拒。相關測試目前共 14 項通過。

### 2026-08-25 Data Action 個別 Rate Limit

已完成 `RATE_LIMIT_PER_MINUTE` 的 Entity、Mapper、Command／View、儲存驗證、Executor 套用與前端設定。留空沿用全域門檻，設定 1～100000 時覆寫該 Action；Rate Limiter 覆寫測試通過，Nuxt production build 與 `git diff --check` 通過。取得使用者明確同意後已執行一次性 migration：本機 `flowmint.fm_data_action` 已新增 nullable `int(11)` 欄位；既有 14 筆 Action 全部維持 `NULL`，未改變任何既有 Action 的有效配額。一次性 migration 檔案依使用者要求於執行後刪除，最終結構保留於 `flowmint.sql`。

文件衝突：`09-程式編排.md` 仍將 `FM_PROG006D0002` 定義為舊使用者中心功能；本次依較新的 `20-動態資料服務規格.md` 與已存在的 `FM_PROG006D0001` 動態資料服務程式編號實作。後續應統一修訂程式編排文件。

後端 compile、Driver Catalog 單元測試、MyBatis XML parse 與前端 production build 已通過。實際連線驗收仍需在套用 `FM_PROG006D0001-schema.sql` 後，分別準備 MariaDB、Oracle、MSSQL 測試環境執行。

## 2026-08-06 FM_PROG005D0001 客製 JavaScript 第一版

已在 fm_form_version 增加 CUSTOM_SCRIPT_CONTENT 規格及 migration SQL，並完成 Entity、Mapper、Command／View DTO、草稿保存、新版本複製、發布鎖定及新版 SHA-256 組合規則。既有 Data Action Demo seed 已同步空白 Script 欄位與 Hash 計算方式。

前端 FM_PROG005D0001 已增加 JavaScript 頁籤、生命週期範本及語法／模組格式檢查。共用 useFormCustomJavascript Executor 支援同步與 async／await，第一版 Context 提供 Form.io instance、submission data、欄位讀寫、redraw、FlowMint Axios instance、Data Action helper、通知及試跑 Console。Designer 試跑已接入 onFormLoad、onFieldChange 與 onDestroy；Executor 同時保留 beforeSubmit、afterSubmit、Data Action 成功／失敗生命週期，供正式發起與簽核 Form Renderer 接入。

驗證結果：前端 npm run build、Mapper XML parse 及 git diff --check 通過。此段是 2026-08-06 當時狀態；後續已建立正式發起與簽核 Form Renderer，並接入同一套 `useFormCustomJavascript` Executor，現況見本文件後續 Runtime 紀錄及 `FM_PROG005D0001_cust_js規劃說明.md` 的「第一版實作狀態」。部署本功能前仍必須先執行 FM_PROG005D0001-custom-script-schema.sql。

## 2026-08-09 FM_PROG004D0001 核決權限持久層

已完成 `fm_approval_authority` 與 `fm_approval_authority_rule` 的 Entity、MyBatis Mapper XML、Mapper 及 Service。兩份 Mapper XML 均明列欄位與 `jdbcType`，提供共用查詢條件、分頁、計數及動態新增、修改、刪除；規則 Service 另提供依 Tenant 與核決權限 ID 排序讀取，以及同一交易內整批替換規則的操作。

本階段已通過兩份 Mapper XML 結構解析、`git diff --check` 與 Maven reactor compile；尚未建立 Logic、Controller，以及 `FM_PROG004D0001` 內嵌的核決權限管理 UI，也未對 MariaDB 執行任何新增、修改或刪除。

## 2026-08-09 核決權限功能整併決策

原規劃的 `FM_PROG004D0004` 不再作為獨立程式、選單或管理畫面。核決權限與條件規則正式併入 `FM_PROG004D0001`：使用者在 BPMN User Task 的簽核人設定中選擇 `APPROVAL_AUTHORITY`，並於同一畫面建立、選擇及編輯規則。既有核決權限資料表、DTO、條件判斷器及持久層繼續保留，作為 `FM_PROG004D0001` 的內部功能與共用資料模型。

## 2026-08-09 FM_PROG004D0001 內嵌核決權限管理

已完成核決權限 Logic 與內嵌 CRUD API，所有端點均掛在 `/api/FM_PROG004D0001/approval-authority/*` 並沿用流程設計程式權限。儲存時會在同一交易驗證 Tenant、流程、權限代碼唯一性、生效期間、規則順序、受控條件 JSON、簽核目標類型及狀態，再整批替換明細規則。

流程設計器的 `APPROVAL_AUTHORITY` 已移除原始 JSON textarea，改用 `ApprovalAuthorityPanel`。使用者可直接在 User Task 屬性面板選擇、新增或編輯核決權限，以結構化條件元件維護 ALL／ANY 條件與多條規則，並由既有帳號、簽核群組、簽核層級、組織職稱及組織職務選項指定簽核目標。本階段已通過 Maven reactor compile、條件判斷器三項單元測試、Nuxt production build 與 `git diff --check`；未異動 MariaDB 資料。`APPROVAL_AUTHORITY` Resolver 的執行期目標解析與動態表單欄位目錄仍待下一階段完成。

## 2026-08-09 FM_PROG004D0001 核決權限 Resolver 與表單欄位目錄

`FmAssignmentResolverService` 已實作 `APPROVAL_AUTHORITY`：依 Tenant 與權限 ID 讀取啟用且在有效期間內的主檔，按規則順序以實際表單變數判斷條件，將命中的簽核層級、組織職稱、組織職務、簽核群組或指定帳號解析為有效員工，並支援候選人去重、`MAX_RESULTS` 及 `STOP_AFTER_APPROVAL`。Resolver Preview Command 已可攜帶表單變數，畫面也可輸入測試表單資料驗證金額或分類條件。

已發布表單選項同步提供該版本的 Form.io Schema；核決條件編輯器會遞迴取得一般元件、Container、Columns 與 Table 內的輸入欄位，不再使用固定示例欄位。驗證結果：Mapper XML parse、Maven reactor compile、條件判斷器三項測試、Nuxt production build 與 `git diff --check` 通過，未異動 MariaDB。此階段完成 Resolver Domain 與 Preview；正式流程任務建立時把 Resolver 結果寫入 Flowable assignee／candidate 的接線仍待後續工作流 Runtime 階段。

## 2026-08-09 Flowable User Task 指派 Runtime 接線

已新增 Spring Bean `fmTaskAssignmentListener`。`FM_PROG004D0001` 發布流程時會在部署用 BPMN 的每個 User Task 自動加入 create TaskListener；資料庫保存及設計器顯示的原始 BPMN 維持不變。每次第一關或後續關卡建立時，Listener 都會依流程 Runtime 變數取得 Tenant、FlowMint 流程 ID、版本、發起人及表單資料，再讀取該發布版本的 Task Policy 與 Assignment Rule，透過共用 Resolver 設定 Flowable assignee 或 candidate users。

目前正式支援 `ASSIGNEE` 與 `CANDIDATE`。`ALL` 與 `SEQUENTIAL` 必須使用 Flowable Multi-instance 才能保證全員會簽或依序簽核，現階段發布檢查會明確拒絕這兩種模式，Listener 也設有防線，不會錯誤地把它們降級成一般候選人。Maven reactor compile、既有核決條件測試及 `git diff --check` 通過，未異動 MariaDB。下一階段需建立正式送單 Runtime Service，負責建立 `fm_form_data`、啟動 Flowable instance 並注入 Listener 要求的標準變數。

## 2026-08-09 正式送單 Runtime Service 核心

已完成 `fm_form_data` 與 `fm_process_instance` 的 Entity、完整 MyBatis Mapper XML、Mapper 與 Service，並建立 `FmProcessSubmitCommand`／`FmProcessSubmitView` 及內部 `FmProcessRuntimeLogicService`。送單交易只接受已發布且具有 Flowable deployment 的流程版本與已發布表單版本，驗證申請人為有效員工且具有有效主要任職，並阻擋重複 business key。

同一 Spring 交易中會建立 SUBMITTED 表單資料、以固定 FlowMint Runtime 變數啟動指定 Flowable process definition，再建立 RUNNING 流程索引。TaskListener 可立即使用 Tenant、流程 ID、版本、申請人與表單資料解析第一關簽核人；任一步驟失敗會回滾 FlowMint 與 Flowable 資料。兩份 Mapper XML parse、Maven reactor compile 與 `git diff --check` 通過，未實際呼叫送單也未異動 MariaDB。此服務目前只作為內部核心，尚未建立對外 Controller；在開放 API 前仍需完成發起人代申請授權、表單 Schema 資料驗證、起單權限政策與送出快照／稽核軌跡。

## 2026-08-09 送單安全：起單權限持久層

已開始依正式順序實作送單安全與完整性，完成 `fm_process_start_policy` 的 Entity、完整 MyBatis Mapper XML、Mapper 與 Service。支援 `ALL`、`ACCOUNT`、`ORG_UNIT`、`APPROVAL_GROUP` 政策資料，以及依 Tenant／流程／版本排序讀取與同一交易整批替換；尚未把政策判斷接入送單 Runtime，也未建立任何政策資料。

Mapper XML parse、Maven reactor compile 與 `git diff --check` 通過。下一步會實作政策比對器與測試，明確定義允許／拒絕優先順序及「沒有政策」時的安全預設，再接入送單交易。

## 2026-08-09 送單安全：起單政策判斷

已新增 `FmProcessStartPolicyEvaluator` 並接入正式送單交易。政策判斷納入申請人帳號、所有有效任職部門及有效簽核群組；沒有政策、沒有命中規則或命中任何 `ALLOW_START=N` 規則時一律拒絕，只有至少命中一條允許且沒有任何拒絕時才可起單。拒絕規則優先，避免較寬鬆的 `ALL` 或帳號允許意外蓋過部門禁止。

四項單元測試涵蓋預設拒絕、帳號／部門／群組允許、ALL 允許及拒絕優先，全部通過；Maven reactor compile 與 `git diff --check` 亦通過，未異動 MariaDB。既有 `fm_workflow_delegation` 是簽核期間代理，不能誤作代申請授權；下一步需建立明確的代起單授權模型，安全預設為本人可送、不同發起人與申請人則必須具備有效專用授權。

## 2026-08-09 Runtime Mapper XML 規範修正

依 `16-QIFU4後端持久層實作規範.md` 重新整理 `FmFormDataMapper.xml`、`FmProcessInstanceMapper.xml` 與 `FmProcessStartPolicyMapper.xml`：查詢欄位 include 改為多行排版，insert 統一使用兩個 `<trim>`，update 統一使用 `<set>`，所有 `<if>` 內容均展開且寫入參數保留明確 `jdbcType`。未增加 copyright header。

`fm_process_instance` 另新增專用 `updateStatus` Mapper／Service，只能在 Tenant、流程實例及目前狀態完全符合時更新目標狀態與結束時間；標準 update／delete 僅為 `IBaseMapper` 相容性保留。三份 XML 結構、dynamic SQL 格式檢查、Maven reactor compile 與 `git diff --check` 通過。

## 2026-08-09 送單安全：代起單授權

已新增專用 `fm_process_start_proxy` 資料表結構、Entity、符合 QIFU4 dynamic SQL 規範的 Mapper XML、Mapper、Service 及 `FmProcessStartProxyEvaluator`。建表結構已合併至 `flowmint.sql`。授權明確區分申請人 `PRINCIPAL_ACCOUNT` 與實際登入發起人 `PROXY_ACCOUNT`，可適用全部流程或單一流程，並限制啟用狀態與有效期間；不使用也不擴張既有簽核代理權限。

送單 Runtime 現在從 Security Context 取得登入帳號，不信任前端傳入發起人。本人送本人直接通過；登入人與申請人不同時，必須命中有效代起單授權。`fm_process_instance.INITIATOR_ACCOUNT` 保存實際發起人，`fm_form_data.OWNER_ACCOUNT` 保存申請人，Resolver 主管鏈仍依申請人解析。四項測試涵蓋本人送單、無授權拒絕、全部／指定流程授權，以及停用／過期拒絕，全部通過；Mapper XML parse、Maven reactor compile 與 `git diff --check` 通過。

`fm_process_start_proxy` 建表結構已納入 `flowmint.sql`；實際 MariaDB 部署狀態需另行確認。

### 2026-08-09 正式送單 Form.io Schema 驗證

已新增 `FmFormSubmissionValidator`，Runtime 送單前會讀取指定的已發布表單版本，驗證必填、型別、長度、數值範圍、pattern、Email、multiple、container 及 DataGrid/EditGrid 明細。Schema 或送出資料無效時，在寫入表單與啟動 Flowable 之前即拒絕。

同時已防止流程與表單錯誤搭配：送出的 `FORM_ID` 與 `FORM_VERSION_NO` 必須存在於該已發布流程版本的 `fm_task_form_rule`，並且 Tenant 與流程版本必須一致。已新增 6 個驗證器單元測試。

### 2026-08-09 送單稽核與 Assignment Snapshot

已依 QIFU4 持久層規範，分別建立 `fm_form_snapshot`、`fm_task_action`、`fm_task_assignment_snapshot` 與 `fm_task_assignment_snapshot_dtl` 的 Entity、繼承 `IBaseMapper` 的 Mapper、完整八區塊 XML、繼承 `IBaseService` 的 Service 及實作。跨表稽核協調放在 `FmRuntimeAuditLogicService`，只依賴 Service，不直接操作 Mapper；不提供修改或刪除不可變稽核資料的 API。正式送單現在會在同一交易內建立 `fm_form_snapshot`，依實際序列化內容計算 SHA-256，並寫入 `fm_task_action` 的 `SUBMIT`。Action 同時保存實際發起人 `ACTOR_ACCOUNT`、申請人 `PRINCIPAL_ACCOUNT`、表單快照與第一關指派快照編號。

Flowable User Task create listener 已於實際指派時寫入 `fm_task_assignment_snapshot` 及明細，保存 resolver 類型、規則路徑、實際候選帳號、部門、職稱對應的簽核層級與解析時間。重新進入同一 Task Definition 時會產生新的 `RESOLUTION_SEQ`，不覆蓋舊快照。下一步是將 Runtime Service 接到正式 Controller，只開放必要的 load 與 submit API。

### 2026-08-09 正式起單 API

已新增 `FmProcessRuntimeController`，只開放 `/api/fm/requests/start/load` 與 `/api/fm/requests/submit`，沒有流程實例刪除端點。Tenant 只從 `X-FlowMint-Tenant` header 取得，發起人只從 Security Context 取得；load 與 submit 都在 Logic 層重新檢查 Tenant membership、在職身分、起單政策與代起單授權。

Submit 必須提供 `Idempotency-Key`，相同 key 重送相同內容會回傳原流程結果，夾帶不同內容時會拒絕。Load 會回傳該已發布流程版本實際綁定的已發布 Form.io Schema、UI Schema、客製 Script 及 Task Definition keys。下一步是正式表單發起 UI 與 Workspace 可發起流程入口。

### 2026-08-09 正式表單發起 UI 與 Workspace

已新增登入後共用的 `/workspace` 工作台與側邊欄入口，一般使用者不必取得後台程式權限即可進入。畫面可選擇登入者具有效 membership 的公司、指定本人或已授權的申請人、查看該申請人實際可發起的流程，並以已發布 Form.io Schema 渲染正式表單。Renderer 同時接入既有 Data Action 與客製 JavaScript runtime；送出後顯示流程編號、表單資料編號及流程狀態。

後端新增唯讀的 `/api/fm/requests/start/tenants` 與 `/api/fm/requests/start/catalog`。Tenant 清單只依 Security Context 查詢；流程清單逐筆套用 Tenant membership、在職身分、代起單授權、起單政策、已發布流程版本與已發布綁定表單檢查，不把不可發起流程暴露給前端。原有 load 與 submit 仍會獨立重做全部安全檢查，submit 保留 `Idempotency-Key`，且仍未提供流程實例刪除端點。

後端 18 項相關測試與 Maven reactor build 通過；前端 `npm run build` 通過。下一步進入待辦與簽核 Runtime：我的待辦、表單與歷次快照檢視、核准／退回／駁回及完整稽核軌跡。

### 2026-08-09 我的待辦與簽核 Runtime

已新增 `/tasks/inbox`、`/tasks/load` 與 `/tasks/action` 正式 Runtime API。待辦清單只包含登入者被直接指派或列為候選人的有效 Flowable Task，並以 FlowMint process instance 再次限制 Tenant 與 `RUNNING` 狀態。待辦詳情提供流程與申請人資訊、節點綁定的已發布 Form.io 表單、原始送單內容、節點政策、歷次 Action，以及同一流程已完成且可作為退回目標的前置 User Task。

簽核動作首版支援 `APPROVE`、`RETURN` 與 `REJECT`。每次動作都重新驗證 Tenant membership、Task assignee／candidate、Task Policy、意見與理由規則；候選人執行時會先原子 claim。核准完成最後一關時同步將 `fm_process_instance` 與 `fm_form_data` 更新為 `COMPLETED`；駁回終止 Flowable instance 並更新為 `REJECTED`；退回只允許移動到同一流程已完成的前置 User Task，透過 Flowable change-state 重新建立該節點及指派快照。因目前 BPMN 尚無申請人補件節點，本版不把退回假裝成申請人修改／重送。

每次核准、退回與駁回都新增不可變 `fm_form_snapshot`，並寫入包含 Actor、Principal、Task、表單快照、當次 Assignment Snapshot、意見與理由的 `fm_task_action`。前端工作台已顯示「我的待辦」清單，並新增唯讀表單與簽核紀錄詳情頁，可依節點政策執行核准、退回或駁回。

Maven reactor compile、18 項既有 Runtime／安全／Mapper 契約測試、前端 `npm run build` 與 `git diff --check` 通過。下一步是補上「我的申請／流程進度」查詢，之後再進入 Multi-instance 的 `ALL` 與 `SEQUENTIAL`。

### 2026-08-09 我的申請與流程進度

已新增 `/mine` 與 `/mine/load` 正式查詢 API。清單同時納入登入者是表單 Owner 的本人申請，以及登入者為實際 Starter 的代發起流程，依起單時間倒序顯示流程、表單、申請人、發起人、目前 User Task 與流程狀態。詳情只允許實際申請人或發起人存取，Tenant 與登入帳號一律取自 header／Security Context，不能由 body 覆寫。

申請詳情提供目前表單內容、完整 `fm_task_action` 軌跡，以及依時間排列的不可變 `fm_form_snapshot`；每份快照包含 Action、Revision、SHA-256 與當次資料，可在 UI 切換比對。Workspace 已加入「我的申請」區塊，支援進行中、完成與駁回狀態及目前節點顯示。

補件與 `RESUBMIT` 尚未假裝完成。現有 BPMN 沒有申請人補件 User Task，若直接把一般簽核 Task 改派給申請人會破壞既有節點政策與稽核語意；下一步應先在流程設計器正式定義申請人補件節點及 Return target，再實作 Returned 表單編輯、Schema 驗證、Revision 遞增、RESUBMIT 快照與流程續行。

## 接續開發順序

### 2026-08-10 申請人補件節點與 RESUBMIT

已將 `APPLICANT_CORRECTION` 納入 User Task 派送模式。流程設計器可將節點標記為「申請人補件」，這類節點不要求 Assignment Rule，Flowable TaskListener 會直接指派給表單申請人。一般簽核節點的退回目標已限制為同一發布版本的補件節點，不再將任意歷史簽核節點當成補件目標。

補件待辦會以可編輯 Form.io Renderer 顯示；`RESUBMIT` 會在後端重新執行已發布 Schema 驗證，更新表單內容、遞增 Revision，同步 Flowable Runtime 表單變數，寫入不可變 `RESUBMIT` 快照與 Task Action，然後完成補件節點使流程繼續。後端 reactor compile、FlowMint 相關測試與前端 Nuxt production build 通過；尚待實際 MariaDB／瀏覽器 E2E 驗收。

### 2026-08-10 ALL／SEQUENTIAL Multi-instance

已完成 `ALL` 與 `SEQUENTIAL` 的真正 Flowable Multi-instance Runtime。發布流程時會依 Task Policy 將 User Task 轉換為平行或循序 Multi-instance，collection expression 會在流程進入節點時使用該發布版本的 Assignment Rule 解析並去重簽核人。`ALL` 同時建立每位簽核人的待辦，全數完成後才離開節點；`SEQUENTIAL` 依 Resolver 結果順序逐筆建立，前一位完成後才產生下一位待辦。

每個 Multi-instance Task 仍由 `fmTaskAssignmentListener` 驗證元素帳號是當次 Resolver 結果，設定獨立 assignee 並寫入 Assignment Snapshot；原本的發布阻擋已移除。新增 BPMN 轉換單元測試，會將產生的 Runtime XML 重新解析，驗證平行／循序設定、collection expression 及 element variable。尚待實際 MariaDB／Flowable 整合與瀏覽器多帳號 E2E 驗收。

### 2026-08-10 Workspace 真實統計與版面整理

Workspace 已以現有正式 Runtime API 資料呈現「我的待辦」、「進行中」、「已完成」與「可發起流程」四張統計卡，數字分別來自 `/tasks/inbox`、`/mine` 與 `/start/catalog`，沒有展示用假數據。待辦與申請清單補上開始時間、目前節點、流程狀態、空狀態及快速導覽；發起區仍保留 Tenant、代申請人與權限過濾語意。

版面已重整為桌面雙欄、行動版單欄，並統一統計卡、狀態標籤、清單層級與焦點區域的視覺語言。Nuxt production build 通過；尚待以真實帳號及多種流程狀態執行瀏覽器視覺驗收。

### 2026-08-10 Phase 4：申請人撤回

已新增 `/mine/withdraw` 正式 Runtime API 與「我的申請」詳情撤回操作。只有表單 Owner 本人可撤回 `RUNNING` 流程；代申請的實際發起人仍可查看進度，但不能代替申請人撤回。撤回原因必填，body 不接受 Tenant 或 Actor，兩者仍分別取自 header 與 Security Context。

撤回交易會鎖定表單資料並重新確認流程狀態，建立不可變 `WITHDRAW` 表單快照與 Task Action，終止 Flowable instance，再以 `RUNNING -> CANCELLED` compare-and-set 更新流程索引及表單狀態。Maven reactor compile、Runtime Controller 契約測試與 Nuxt production build 通過；未操作 MariaDB 實際資料。下一個 Phase 4 切片是定義並實作 `CANCEL` 的角色與適用情境，避免與申請人 `WITHDRAW` 混用。

### 2026-08-10 Phase 4：實際發起人取消流程

已新增 `/mine/cancel` API，將 `CANCEL` 與 `WITHDRAW` 的權責分開：表單 Owner 使用 `WITHDRAW`；流程的實際發起人使用 `CANCEL`，主要處理代申請發起人需要終止自己啟動流程的情境。兩者共用相同的併發鎖定、Flowable 終止、`RUNNING -> CANCELLED` compare-and-set 與表單狀態更新，但分別寫入 `WITHDRAW` 或 `CANCEL` 不可變快照及 Action。

我的申請詳情頁只在登入者是代申請的實際發起人、且不是表單 Owner 時顯示「取消代申請流程」，避免同一畫面同時出現語意重疊的撤回與取消。原因必填且最多 1000 字；Tenant 與 Actor 仍不能由 body 覆寫。33 項 FlowMint 測試與 Nuxt production build 通過，未操作 MariaDB 實際資料。下一個 Phase 4 切片進入 Task Transfer。

### 2026-08-10 Phase 4：Task Transfer

已新增 `/tasks/transfer-options` 與 `/tasks/transfer`。只有目前 assignee／candidate 且 Task Policy `ALLOW_TRANSFER=Y` 的使用者可以讀取轉派對象或執行轉派；目標必須是同 Tenant、啟用且在有效期間內的員工，並具有有效 Tenant membership，不能轉派給自己。原因必填且最多 1000 字，Tenant 與 Actor 仍分別由 header 與 Security Context 決定。

轉派不完成 User Task，也不推進流程。若操作者是 candidate，會先以 Flowable 原子 claim 取得 Task；接著移除原 candidate user／group links 並設定唯一新 assignee，避免舊候選人繼續存取。稽核會建立 resolver type 為 `TRANSFER` 的新 Assignment Snapshot，保存來源與目標帳號，再建立不可變 `TRANSFER` 表單快照及 Task Action。待辦詳情已依 `allowTransfer` 顯示轉派操作與有效員工選項。34 項 FlowMint 測試與 Nuxt production build 通過；未操作 MariaDB 實際資料。下一個 Phase 4 切片進入 Delegation／Resolve。

### 2026-08-10 Phase 4：Delegation／Resolve 核心

已新增 `/tasks/delegate` 與 `/tasks/resolve`，正式使用既有 `fm_workflow_delegation`，不新增或擴張代起單授權。委託時不接受任意目標帳號，只能選擇登入者作為 principal、目前有效且適用 `ALL` 或指定流程 scope 的既有代理授權；代理人也必須是同 Tenant 的有效員工與有效 membership。Flowable `delegateTask` 會保留原 owner 並將 assignee 改為代理人，代理人完成處理後使用 `resolveTask` 回覆原 owner，不會直接完成 User Task 或推進流程。

代理狀態中的 Task 已禁止直接核准、退回、駁回或重送；若再次委託，必須由原 owner 對目前代理人的有效授權明確設定 `ALLOW_REDELEGATE=Y`。`DELEGATE` 與 `RESOLVE` 都建立新的 Assignment Snapshot、不可變表單快照及 Task Action。待辦頁會顯示適用代理授權，代理人則看到「回覆委託人」。Delegation 已支援 `ALL`、`PROCESS` 與 `APPROVAL_GROUP` scope；群組 scope 會依流程實例的版本、目前 Task Definition Key 與啟用的 `APPROVAL_GROUP` Assignment Rule 精確比對 `approvalGroupId`，失效規則、其他節點、其他 resolver 或無效 JSON 均不適用。相關判斷已有獨立單元測試；未操作 MariaDB 實際資料。

已完成首版循序前加簽。原處理人只能在 Task Policy `ALLOW_ADD_SIGN=Y` 時選擇同 Tenant 有效員工，且不可對自己或代理／加簽中的 Task 再加簽；加簽期間流程停留在原節點，加簽人只能「完成加簽」並將 Task 還給原 owner，不能核准、退回、駁回或誤用一般 Resolve。`ADD_SIGN` 與 `ADD_SIGN_COMPLETE` 分別建立 Assignment Snapshot、不可變表單快照及 Action，待辦頁已提供加簽人選擇及完成加簽操作。後端 39 項 FlowMint 測試與 Nuxt production build 通過；未操作 MariaDB 實際資料。

Approval Group 三種多人模式已形成發布與 Runtime 閉環：`CANDIDATE` 建立一個共用候選 Task，`ALL` 建立每位有效成員各自處理的並行 multi-instance Task，`SEQUENTIAL` 依群組成員優先序逐一建立 Task。流程發布前會讀取同 Tenant 啟用群組，要求 `APPROVAL_GROUP` Assignment Rule 所在節點的 Task Policy 派送方式與群組 `ASSIGNMENT_MODE` 完全一致；群組不存在、停用、設定 JSON 無效或模式不一致都禁止發布，避免設計值與實際執行語意分歧。

Incident operations 已完成第一個必要基礎：Task Assignment Listener 遇到 Resolver 無法解析時，不再直接拋錯使整個 Flowable 交易與異常證據一起回滾；現在會保留停在原節點的未指派 Task，寫入 Tenant-scoped `fm_assignment_incident` `OPEN` 紀錄，包含流程、Task、節點、錯誤碼、截斷後錯誤訊息及解析 Context，並在 Task category 保存 Incident ID 供營運操作精確關聯。Incident 寫入失敗仍會使原交易回滾，避免留下無 Incident 的孤兒 Task。下一切片接續 Incident 查詢、Retry／Reassign、Terminate 與管理員稽核 API。

Incident 管理 API 已完成 Tenant-scoped 清單查詢與管理員 Reassign。僅系統管理員或 `FLOWMINT_OPERATIONS` 角色可使用；查詢限制合法 Incident status。Reassign 強制要求理由並驗證 Incident 仍為 `OPEN`、Task category 精確關聯、流程仍在執行、Task 非代理狀態，以及目標員工與 Tenant membership 均在有效期間；成功後清除原候選、設定唯一 assignee、建立 `ADMIN_REASSIGN` Assignment Snapshot／不可變表單快照／Task Action，再以 `OPEN` 條件更新 Incident 為 `RESOLVED`，並保存處理人、時間與理由。尚待 Retry、Terminate 與營運畫面。

Incident Retry 與管理員 Terminate API 已完成。Retry 會依 Incident 對應流程版本與 Task 節點重新執行所有啟用 Assignment Rules，要求每條規則成功且至少解析一位有效簽核人，再依 `ASSIGNEE`、`CANDIDATE`、`ALL` 或 `SEQUENTIAL` 恢復 Flowable 指派；成功後建立 `INCIDENT_RETRY` Assignment Snapshot、不可變表單快照與 `ADMIN_REASSIGN` 稽核 Action，最後以條件更新關閉 Incident，失敗則整個交易回滾並維持 `OPEN`。Terminate 強制理由並驗證 Tenant、業務流程與 Flowable instance 都仍在執行，建立 `TERMINATE` 快照／Action 後終止 Flowable，將流程轉為 `TERMINATED`、表單轉為 `CANCELLED`，同流程其餘 `OPEN` Incident 轉為 `IGNORED`。Phase 4 後端操作閉環完成，尚待營運管理畫面。

Incident 營運管理畫面已完成，路由為 `/operations/incidents`。畫面支援 Tenant 與 `OPEN`／`RESOLVED`／`IGNORED` 狀態篩選、錯誤與格式化 Context 明細、有效員工下拉選擇、Retry、管理員 Reassign、Terminate 二次確認，以及已處理人、時間與註記顯示；權限仍由後端強制檢查。新增受權限保護的 Reassign options API，避免管理員手寫帳號。後端 47 項 FlowMint 測試、Nuxt production build 與 `git diff --check` 均通過，未操作 MariaDB 實際資料。至此 Phase 4 開發項目完成；瀏覽器 E2E 驗收仍依既定決定獨立延後，不混同為開發完成條件。

下次接續開發時，應依下列順序進行，不得把尚未完成的項目誤判為已完成：

1. Phase 5 營運功能：Notification 通知中心、通知範本與發送紀錄、流程實例監控、完整稽核軌跡查詢、營運報表，以及排程、逾時與提醒機制。
2. 完整瀏覽器端流程驗收：實際驗證登入、F5 還原、起單、代申請、待辦、核准、退回、補件、駁回、撤回、完成、歷程與快照。
3. 實作請購單正式表單與 Grid 明細：包含請購分類、發起人／申請人、品項明細、單價、數量、金額及總金額；依最新決策延後至 Phase 5 核心營運能力完成後。

### 2026-08-11 Phase 5：站內通知中心第一階段

已使用既有 `fm_notification` 建立 Entity、完整 QIFU4 Mapper、Service、Logic 與 `/api/fm/notifications` Runtime API。登入者可依目前 Tenant 查詢最近 100 筆站內通知及未讀數、標記單筆已讀或全部已讀；Tenant 與收件帳號不能由 request body 覆寫，所有查詢及更新都同時限制 `TENANT_ID`、登入 `ACCOUNT` 與 `IN_APP` channel。

Workspace 已加入未讀通知統計與通知中心，可重新整理、標記單筆或全部已讀。後端 Maven reactor compile、通知 Mapper parser／Controller contract 測試與前端 Nuxt production build通過。此階段只完成收件匣讀取閉環；通知事件產生、通知範本、Email 發送、重試排程與發送紀錄仍待後續切片，不得標記為完整 Notification 完成。

### 2026-08-11 Phase 5：待辦指派通知事件

Flowable Task Assignment Listener 已在指派快照成功後產生 `TASK_ASSIGNED` 站內通知，涵蓋申請人補件、單人指派、共用候選人、平行及循序 Multi-instance。通知收件人使用 Resolver 最終解析出的登入帳號，reference 保存實際 Flowable Task ID，Workspace 可讀取並標記已讀。

通知事件使用 `Tenant + Event Type + Task ID + Recipient Account` 產生穩定 UUID，Mapper 以 `INSERT ... WHERE NOT EXISTS` 再次去重；Listener 重入或 Incident Retry 不會為同一 Task 與收件人建立重複通知。通知寫入與 Task 指派／快照位於相同交易，通知持久化失敗會使指派交易回滾，避免流程已產生待辦但通知資料永久遺失。本切片尚未包含流程完成／駁回／撤回通知，也尚未實作 Email channel。

### 2026-08-11 Phase 5：流程狀態通知與 QIFU4 Mail Outbox

流程最後一關核准完成、駁回、申請人撤回及實際發起人取消時，會通知表單 Owner 與實際 Starter；兩者相同時自動去重。事件分別保存為 `PROCESS_COMPLETED`、`PROCESS_REJECTED` 或 `PROCESS_CANCELLED`，reference 指向流程實例。

Email 不另建 SMTP client 或寄信排程，而是依 QIFU4 既有機制建立 `TbSysMailHelper`，交由 `SendMailHelperJob` 寄送及重試。收件地址取同 Tenant、啟用且在有效期間內的 `fm_employee.EMAIL`；沒有 Email 時只保留站內通知，不阻斷流程。每封待寄 Email 另寫入 `fm_notification` 的 `EMAIL/PENDING` 紀錄並使用穩定事件 ID 去重，`TbSysMailHelper.RETAIN_FLAG=Y` 以保留 QIFU4 寄送結果。尚待補上 QIFU4 寄送結果回寫 `fm_notification` 的 `SENT/FAILED` 狀態同步，以及可管理的通知範本。

### 2026-08-11 Phase 5：QIFU4 通知範本整合

通知主旨與內容已集中至 `FmNotificationTemplateCatalog`，並對應 QIFU4 `tb_sys_template`／`tb_sys_template_param`：`FMTASKASG`、`FMPROCMP`、`FMPROREJ`、`FMPROCAN`。通知範本已匯入資料庫，完成部署後已移除一次性 SQL。

Runtime 優先使用 QIFU4 `TemplateUtils` 與 FreeMarker 參數產生站內通知及 Email 的相同內容；範本資料不完整或渲染失敗時會記錄 warning 並使用內建安全文字，不使簽核交易失敗。範本目錄與 fallback 已有單元測試。

### 2026-08-11 Phase 5：Email 寄送結果同步

`fm_notification.PROVIDER_MESSAGE_ID` 已套用至資料庫，以該欄位精確保存 QIFU4 `TbSysMailHelper.MAIL_ID`，避免以收件人、主旨或內容猜測寄送關聯。完成部署後已移除一次性 migration SQL。

`FmNotificationDeliverySyncJob` 每三分鐘批次讀取 `EMAIL/PENDING` 通知，找到 QIFU4 保留且 `SUCCESS_FLAG=Y` 的 Mail Helper 後，以 Tenant、Notification ID、Provider Message ID 及目前狀態條件更新為 `SENT`，寄送時間使用 QIFU4 `SUCCESS_TIME`。現有 QIFU4 Mail Helper 沒有永久失敗欄位，故本階段不以時間猜測 `FAILED`；若要精確失敗同步，須先擴充核心寄信資料的最終失敗狀態與錯誤訊息。

### 2026-08-11 Phase 5：流程實例監控第一階段

新增營運查詢 `/api/fm/operations/process-instances` 與 `/operations/processes` 頁面。只有系統管理員或 `FLOWMINT_OPERATIONS` 可查詢，且資料固定限制 `X-FlowMint-Tenant`。支援 `RUNNING`、`COMPLETED`、`REJECTED`、`CANCELLED`、`TERMINATED` 狀態與流程編號、業務單號、流程名稱、申請人及發起人關鍵字。

新增 `/api/fm/operations/process-instances/load` 流程監控明細。營運人員可由監控清單查看依時間排序的任務動作與表單快照（含 revision、內容雜湊與當時表單資料）；查詢沿用營運角色檢查，且流程、動作、快照都以 Header Tenant 與流程實例 ID 雙重限制，請求本文不能覆寫 Tenant 或操作者。

監控清單顯示流程版本、Owner、實際 Starter、目前 Flowable User Task、開始及結束時間；既有管理員 Terminate 仍沿用 Incident Operations 的受控 API，本頁不新增可繞過理由、稽核或狀態鎖定的終止操作。流程明細稽核、流程耗時、最近 Task 期限與逾時 Task 數量已補齊。清單已改為 10／30／50／100 筆後端分頁，狀態及 escaped 關鍵字條件下推至 MyBatis count／page SQL，不再先載入 Tenant 全部流程後於 Java 過濾。

### 2026-08-11 Phase 5：Task SLA 與期限提醒第一階段

User Task Policy 新增處理期限小時數與提前提醒小時數；流程設計器可在草稿版本逐節點設定。Task 建立時依曆時小時設定 Flowable due date；未設定期限的節點不啟用提醒。後端限制期限為 1 至 8760 小時，提前提醒必須大於等於 0 且小於期限。工作日曆仍屬暫不排程範圍。

`FmTaskDeadlineNotificationJob` 每五分鐘分頁掃描有 due date 的有效 Task，在進入提前提醒區間時產生 `TASK_DUE_SOON`，逾期後產生 `TASK_OVERDUE`。收件人取目前 assignee 與 candidate users，通知沿用既有站內通知、QIFU4 Mail Outbox、穩定事件 ID 去重與 `FMTASKDUE`／`FMTASKOVD` 範本。Task SLA 欄位及通知範本已套用至資料庫，完成部署後已移除一次性 SQL。

### 2026-08-11 Phase 5：基本營運報表

新增 `/api/fm/operations/reports/summary` 與 `/operations/reports` 頁面，僅系統管理員或 `FLOWMINT_OPERATIONS` 可使用。報表固定限制 Header Tenant，日期採 `yyyy-MM-dd`、預設最近 30 天且最多 366 天。流程總數、各狀態數及完成流程平均耗時由單一聚合 SQL 計算；逾時 Task 與未來 24 小時內到期 Task 由 Flowable Tenant process variable 條件直接 count，不載入整批流程或 Task 明細。

報表已加入完整日期序列的每日趨勢，分別依 `START_DATE` 統計起單量、依已完成流程的 `END_DATE` 統計完成量及當日完成平均耗時；沒有資料的日期補零。後端以聚合 `UNION ALL` SQL 一次取得趨勢，前端以表格與比例條呈現，不引入額外圖表套件。

報表亦加入依流程定義聚合的前 20 名排名，顯示區間內起單量、完成量、完成率與平均完成耗時。完成率以同區間起單 cohort 的目前完成狀態計算，讓尚在執行中的流程自然反映於比率；SQL 固定 Tenant、日期區間並限制 20 筆。

節點效能排名使用同 Task 的最早 Assignment Snapshot `RESOLVED_DATE` 到實際完成型 Action `ACTION_DATE` 計算處理時間，納入 `APPROVE`、`REJECT`、`RETURN`、`RESUBMIT`，排除不完成 Task 的轉派、委託、加簽及註記動作。依流程定義與 Task Definition Key 聚合完成樣本數、平均及最長耗時，固定回傳平均耗時最高的前 20 名；未完成逾時 Task 仍由 Runtime SLA 指標呈現，不混入歷史完成耗時。

### 2026-08-11 Phase 5：營運 Program 正式入口

新增 `FM_PROG008D-register.sql`，註冊流程營運 Folder，以及實際存在的流程實例監控 `FM_PROG008D0001Q`、指派異常處理 `FM_PROG008D0002Q`、流程營運報表 `FM_PROG008D0004Q`。SQL 採 `NOT EXISTS` 可重複執行，且不自動授予角色；部署後須透過 QIFU4 權限管理將需要的 Query Program 配置給 `FLOWMINT_OPERATIONS`。完整稽核目前整合於流程監控明細，通知中心整合於 Workspace，依「只有實際 UI Page 才註冊 Page Item」規則不建立虛構選單項目。

### 2026-08-13 請購單純日期欄位實作決策

`FM_PURCHASE_REQUEST` 的申請日期與期望交貨日屬於純日期，不包含時間與時區語意。這類欄位統一使用 Form.io 基礎 `input` component 搭配瀏覽器原生 `inputType=date`，資料格式固定為 `yyyy-MM-dd`；不使用 Form.io `datetime`／Flatpickr。

此決策避免 Flatpickr instance 在 Form Designer 試跑、submission 重設或客製 JavaScript 生命週期中被重建，造成日期看似選取但值無法保留。後端 `FmFormSubmissionValidator` 已將 `input` 納入文字型別驗證。未來新增純日期欄位應沿用原生 date；只有確實需要日期加時間、時區顯示或時間選擇時，才使用 `datetime`。

### 2026-08-14 請購單流程規劃

已新增 `23-請購單流程與表單配置規格.md`。請購流程採「採購類別與風險決定專業審查、含稅總金額決定高階核決」的雙維度模型；所有案件均須經部門主管、必要專業審查、採購商務審查及財務審查。`totalAmount` 不超過 300,000 元於財務後完成，超過 300,000 至 500,000 元加總經理，超過 500,000 元再加董事長。此階段僅完成規劃文件，尚未建立或發布 MariaDB 流程資料。

後續分析已將規格擴充為五維決策模型：採購類別、風險屬性、預算與支出性質、採購方式與例外、金額與重大投資門檻。文件同時補上請購與採購履約流程邊界、預算預檢、平行專業會簽、重大投資／董事會治理、補件後核准失效矩陣、現有表單欄位缺口、版本策略及五組必要驗收案例。現有 `FM_PURCHASE_REQUEST` 欄位不足以直接建立完整正式流程，必須先確認公司治理參數並補齊表單資料。

### 2026-08-14 請購單流程分流欄位第一批

已直接更新 MariaDB `A01` Tenant 的 `FM_PURCHASE_REQUEST` Version 1 草稿。請購分類擴充為 11 類，新增「支出與預算」、「採購方式與例外」及「風險與專業審查問卷」三個區塊，涵蓋申請類型、CAPEX／OPEX、預算狀態、成本中心、專案總額、採購方式、單一來源、緊急採購、新供應商、關係人、進口採購及八項風險旗標。客製 Script 新增專案總額、預算編號、單一來源理由與緊急原因驗證。

異動 SQL 為 `FM_PURCHASE_REQUEST-routing-fields.sql`。正式執行前已以相同 transaction rollback 試跑，確認精確命中一筆；commit 後回查 Schema JSON、欄位路徑、四項 Script 驗證及內容 SHA-256 均正確。表單目前仍為 `DRAFT`，設備工程明細、外幣、付款條件、重大投資效益、附件用途分類及正式 BPMN 尚待後續批次完成。

### 2026-08-14 請購單設備、付款與投資欄位第二批

已直接更新 MariaDB `A01` Tenant 的 `FM_PURCHASE_REQUEST` Version 1 草稿，新增「設備／工程與驗收資料」、「外幣與付款條件」及「CAPEX 投資效益」三個條件區塊。設備類與施工案件可維護安裝地點、技術規格、驗收標準、產能、公用需求、維護、保固、訓練及使用年限；幣別擴充 TWD、USD、EUR、JPY、CNY，新增匯率、TWD 換算總額、跨年度、預付款及付款條件；CAPEX 可維護投資原因、效益、回收年限、NPV、IRR、產能／品質改善與不投資風險。

客製 Script 已同步計算 `totalAmountTwd`，並驗證匯率、TWD 專案總額、預付款比例、設備／工程必要規格與驗收標準，以及 CAPEX 投資原因與效益。異動 SQL 為 `FM_PURCHASE_REQUEST-equipment-finance-fields.sql`；正式更新前已 rollback 試跑，commit 後 Schema JSON、SHA-256、欄位路徑與 JavaScript 語法均通過。表單仍為 `DRAFT`；附件用途分類因需擴充每個附件的 metadata、API 與 Workspace，未以單一表單欄位假裝完成。

### 2026-08-14 請購流程簽核群組與成員

已直接在 MariaDB `A01` Tenant 建立 11 個 `PURCHASE_*` 簽核群組及 26 筆有效成員。製程與設備、工安與環保分別拆成獨立群組，避免 `CANDIDATE` 模式使不同專業互相取代；資訊群組排除 `admin`／`tester` 測試性帳號。一般專業、總務、採購與財務群組採 `CANDIDATE`，重大投資審議採 `ALL`，成員為研發工程處、生產製造處、供應鏈管理處、財務管理處及總經理。所有 26 名成員均回查為有效員工及有效 Tenant membership。

合約／法務群組因目前組織資料沒有明確責任單位，以 `INACTIVE` 空群組建立，不得用於正式流程。董事長不放入重大投資群組，後續應以獨立董事長核決 Task 處理。異動 SQL 為 `FM_PURCHASE_APPROVAL-groups.sql`；正式執行前已 transaction rollback 試跑，確認新增 11 個群組與 26 名成員後才 commit。

### 2026-08-15 共用單據編號資料表

已新增 `24-共用單據編號規格.md`，並在本機 `flowmint` MariaDB 建立 `fm_document_number_rule` 與 `fm_document_sequence`。兩表均使用 InnoDB、`utf8mb4_unicode_ci`、Tenant 複合唯一鍵、Audit 欄位與必要 CHECK 約束；建立後回查結構與約束正確，兩表目前皆為 0 筆，未建立請購規則或異動既有資料。相同 DDL 已同步納入 `flowmint.sql`。

本階段只完成資料模型。`fm_process_def.DOCUMENT_TYPE`、Entity、Mapper、Service、併發安全取號、請購規則 Seed、Runtime 與 Workspace 顯示仍待後續實作；現行送單仍使用既有 `BUSINESS_KEY`／UUID 行為。

### 2026-08-15 請購單編號欄位

已更新 MariaDB `A01` Tenant 的 `FM_PURCHASE_REQUEST` Version 1 草稿，在「申請資訊」最前方新增唯讀請購單編號欄位。最初欄位代碼為專用的 `purchaseRequestNo`，後續平台化規格已統一改為 `documentNumber`；欄位不可由使用者修改且不保存為一般 submission data。

本階段當時只加入表單顯示欄位。後續「共用單據編號 Runtime」已完成流程 `DOCUMENT_TYPE` 綁定、取號服務、資料模型分離及 Runtime Renderer 注入；目前剩餘的是請購流程／表單發布與真實送單 E2E，不能只憑草稿欄位宣稱正式請購單號已上線。

### 2026-08-15 共用單據編號 Runtime

已完成 `fm_document_number_rule` 與 `fm_document_sequence` 的 Entity、Mapper、Service，以及共用 `FmDocumentNumberService`。取號依 Tenant 時區計算 `NONE`／年／月週期，以 Tenant、單據類型與週期鎖定 sequence row，原子遞增 `CURRENT_NO` 與 `LOCK_VERSION`，並只接受受控格式 token。`fm_process_def` 已增加 `DOCUMENT_TYPE`，`A01／FM_PURCHASE_APPROVAL` 已綁定 `PURCHASE_REQUEST`；請購規則為 `PR-{TENANT}-{YYYY}{MM}-{SEQ}`、月重設、六位流水號。

正式送單已將 `Idempotency-Key` 與 `BUSINESS_KEY` 分離：`fm_form_data.IDEMPOTENCY_KEY` 保存請求冪等鍵，正式單號保存於流程索引及 Flowable business key；相同冪等鍵重送會回傳原單號，不再次取號。待辦與申請追蹤 Renderer 統一把 Runtime business key 注入唯讀 `documentNumber`，不再為不同表單硬編碼專用 Vue 欄位。未設定 `DOCUMENT_TYPE` 的非正式流程維持 UUID，相反地，已設定正式單據類型但規則缺漏時會拒絕送單，不降級 UUID。

`A01／FM_PURCHASE_REQUEST／Version 1` 草稿已執行 `FM_PURCHASE_REQUEST-document-number-key.sql`，將既有 `purchaseRequestNo` 精確遷移為平台共用 `documentNumber`。完整 transaction rollback 試跑確認兩段 UPDATE 各影響 1 筆後才 commit；回查 Schema JSON 有效、舊 key 不存在、新 key 位於原欄位位置，`CONTENT_SHA256` 已重算為 `967bb5a34539a3135e93a284ee67c84cb827c43d426446f55b755108ff4c9db4` 且與即時計算值一致。表單仍為 `DRAFT`，本次未發布。

Java 與 Vue 已完成請購單硬編碼稽核及共用化重構。Task Inbox 與申請追蹤 API 新增明確 `documentNumber`，只在流程配置 `DOCUMENT_TYPE` 時回傳，不再由 Vue 將所有 business key 推測為單據編號。Java 以 `FmSystemFormFields`、Vue 以 `FLOWMINT_SYSTEM_FIELDS` 集中管理系統欄位；表單設計器可一鍵加入單據編號與申請人情境欄位。Workspace 自動帶入申請人帳號，起單與補件送出前會移除僅供顯示的 `documentNumber`。Java 411 個來源檔編譯成功、11 項目標測試通過，Nuxt production build 通過；本次重構未異動 MariaDB。

Runtime 已進一步完成 `businessKey` 與 `documentNumber` 的資料模型分離：新送單以獨立 UUID 作為 Flowable／FlowMint business key，單據編號則由規則服務另行產生並保存、搜尋及回傳。`fm_form_data`、`fm_process_instance` Entity 與 Mapper 已增加 `documentNumber`，監控關鍵字搜尋同時涵蓋兩者，送單結果及 Workspace 優先向使用者顯示可讀單號。Migration `FM-business-key-document-number-separation.sql` 已執行；兩表的 `BUSINESS_KEY` 維持 NOT NULL 與 Tenant 唯一索引，新增 nullable `DOCUMENT_NUMBER` 及 Tenant 唯一索引。執行時兩張 Runtime 表皆為 0 筆，未回填或修改任何既有 business key。Java 14 項目標測試與 Nuxt production build 通過。

資料庫 migration `FM-document-number-runtime-schema.sql` 已執行並回查：新增兩個欄位、兩個索引、1 筆請購編號規則及 1 筆流程綁定，`fm_document_sequence` 仍為 0 筆，尚未消耗正式號碼。後端 compile 與 11 項目標測試通過，前端 Nuxt production build 通過；完整後端 suite 的 FlowMint 測試通過，既有 `MqttIntegrationTest` 因測試環境缺少 `MqttConfigProperties` Bean 失敗，與本功能無關。請購流程與表單仍為 `DRAFT`，尚待發布及真實送單 E2E。

### 2026-08-19 設計時發布驗證補強

已新增 Tenant 存取守門、表單 Schema／欄位目錄驗證、Custom JavaScript 契約驗證、BPMN 結構驗證及 Assignment Rule 參數驗證，並補上流程發起政策的 Command／View 與流程設計器操作介面。發布前檢核現在會更早拒絕跨 Tenant、未知表單欄位、不合法 Script 契約、BPMN 結構錯誤及不完整 Resolver 設定，避免錯誤設計進入 Runtime。

本次同步新增上述 Domain Validator 的單元測試，並調整既有 Multi-instance BPMN 測試。平台開發重點已由基本 Runtime 功能轉為發布安全與正式請購流程上線前收斂；請購流程與表單仍維持 `DRAFT`，尚未完成瀏覽器驗收、正式發布及真實送單 E2E。

### 2026-08-20 已知 Runtime 缺陷修正完成

2026-08-19 全面檢視列出的七項已知缺陷已完成程式修正：Custom JavaScript 與 Data Action Bridge 卸載競態、非同步 `onFieldChange` 事件遺失、response source 缺漏、response／status／error target 衝突、表單發布時 Data Action catalog／metadata 交叉驗證，以及 Custom JavaScript lifecycle timeout。

Custom JavaScript 現在以 `destroying`／`destroyed`、generation token、latest pending change 與預設 15 秒 timeout 控制過期操作；Data Action response mapping 會先完整驗證再套用。表單發布會確認同 Tenant 啟用 Action、發布版本、request mapping keys 與 response result keys。後端發布驗證與 runtime 都會拒絕 target 用途衝突。

本批修正已通過完整 FlowMint 後端測試 115 項（0 failures、0 errors）、Nuxt production build 與 `git diff --check`。目前尚待完成的是前端非同步自動化測試、真實瀏覽器 E2E、MariaDB／Flowable 多帳號流程、Assignment Resolver 三方一致性回歸、QIFU4 郵件永久失敗狀態契約，以及正式請購發布驗收。請購流程與表單仍維持 `DRAFT`。

依產品決策，Custom JavaScript 未做真正執行隔離屬受信任擴充模型，不認定為 bug；後端授權、tenant 邊界、版本治理、timeout 與過期結果禁止回寫仍須保留。同一 Form.io event 是否允許多個 Data Action 仍待產品決策。

### 2026-08-20 請購流程正式發布前準備完成

`A01／FM_PURCHASE_REQUEST／Version 1` 已為 `PUBLISHED`。`A01／FM_PURCHASE_APPROVAL／Version 1` 已在 MariaDB 完成 BPMN 與任務設定補強，但仍維持 `DRAFT`，尚未建立 Flowable deployment，也尚未正式發布。

流程目前有 15 個 User Task 與 31 條 Sequence Flow。新增 `contractReview`、`applicantCorrection` 與 `investmentReview`；無法判定的分類及 `OTHER` 會進入申請人補件，補件重送後回到 `departmentApproval` 重新分流。合約／保密風險會進入 `PURCHASE_CONTRACT_REVIEW`；該群組已改為 `ACTIVE／CANDIDATE`，暫由行政管理處郭志強 `fm00800` 與邱怡蓉 `fm00800e01` 處理。

第一版重大投資門檻已確認為 `expenseType == "CAPEX" && projectTotalAmount > 10000000`；`10,000,000` 元整不觸發，`10,000,000.01` 元起進入 `PURCHASE_INVESTMENT_REVIEW`。該群組為 `ACTIVE／ALL`，5 名有效成員均須完成審議。

資料庫 BPMN SHA-256 為 `103c77e03844cb61a5cda2052511ebbf80b9e3488cb8deeb30cfff64d9eee958`。發布前唯讀檢查已確認：Java `FmBpmnDesignValidator` 與 Flowable parser 通過、15 筆表單規則與 15 筆 Task Policy 一對一、14 筆一般任務指派有效、Gateway 使用的 12 個欄位均存在於已發布表單、所有審查群組及高階核決帳號有效、140 筆有效主要組織指派均有部門主管、Runtime 流程為 0 筆。

下一個明確步驟是經 user 授權後，必須透過 `FmProcessDefLogicServiceImpl.publish()` 正式發布，不得只用 SQL 修改狀態。發布後才進行真實瀏覽器與 MariaDB／Flowable 多帳號 E2E；目前不可宣稱請購流程已上線。

### 2026-08-21 請購流程發布與 Workspace／申請中心重構

`A01／FM_PURCHASE_APPROVAL` 已完成正式發布；上一節所載「仍為 DRAFT、尚待發布」是 2026-08-20 當時的歷史狀態，不是目前狀態。發布後曾發現 `admin` 在 A01 重新登入後仍看不到可發起流程；原因是 Runtime Catalog 將已發布的流程與表單錯以 `ACTIVE` 狀態查詢。`FmProcessRuntimeLogicServiceImpl` 已改為依 `PUBLISHED` 查詢，並保留 Tenant membership、申請人與啟動規則的後端驗證。

Workspace 起單 UX 已完成平台化重構：`pages/workspace.vue` 不再包含流程下拉選單、Form.io、附件與送出程式，改為工作摘要、待辦、最近申請、通知與「前往申請中心」的明顯入口。新增 `/requests/start` 申請中心，依後端回傳的 category code、label、icon 與 sort order 分類顯示可發起流程，支援搜尋、類別篩選、Tenant 切換及本人／有效代理申請人受控選擇。

新增 `/requests/start/[processDefId]` 獨立填單頁，Form.io 不再顯示在 Workspace 下方。共用 composable 已拆分 Catalog、Runtime Form、附件與 Submit 責任，支援 Custom JavaScript、Data Action、系統欄位注入、必要附件、上傳／刪除、Dirty 離頁提示、重複送出防護、Idempotency Key 與成功後導向申請明細。

後端新增受控類別 Catalog 與 `/api/fm/requests/start/applicants` API。Applicant API 回傳本人與目前操作者具有有效代理授權的申請人，並附帳號、顯示名稱、主要部門與本人標記。Catalog、Load 與 Submit 仍各自重新驗證 JWT 使用者的 Tenant 成員資格、有效員工、代理授權、流程發布狀態、啟動規則與表單綁定。

本次已重複掃描 Workspace、申請中心、填單頁與 Runtime 共用模組，沒有 `FM_PURCHASE_APPROVAL`、`FM_PURCHASE_REQUEST` 或依 process key 分支的請購專用邏輯。Java 中 `PURCHASE` 僅存在於受控的通用類別 metadata，新增流程不需修改 Workspace 或填單頁程式。

驗證證據：後端 `FmProcessCategoryCatalogTest` 與 `FmProcessRuntimeControllerContractTest` 共 11 項通過，Nuxt production build 通過，`git diff --check` 通過。尚未完成的是真實環境瀏覽器 E2E：需啟動 MariaDB、後端與前端，以 `admin／A01` 及一般員工驗收本人送單、代申請、附件、Custom JavaScript、Data Action、實際 Flowable 流轉、單據編號與手機畫面。未完成這些步驟前，只能標記「Java／Vue 實作與編譯驗證完成」，不可標記「實機驗收完成」。

產品決策：不實作 Start Context API。Tenant、Applicant 與 Process Def ID 僅作為 route state，不是授權憑證；使用者即使自行修改 URL，後端仍必須依 HttpOnly Cookie 內的 JWT 與請求當下資料重驗。Start Context 不列為本次上線條件，後續不得未經新的產品決策將它加回。

下一個明確步驟：啟動完整環境後執行上述瀏覽器／MariaDB／Flowable E2E；若驗收發現缺陷，再依實際證據修正 Java 或 Vue。

### 2026-08-22 流程分類 Metadata 去寫死重構

上一版 `FmProcessCategoryCatalog` 在 Java 靜態 Map 寫死 `HR`、`ADMIN`、`PURCHASE`、`FINANCE`、`IT` 與 fallback `OTHER`；這雖不包含請購送單邏輯，仍違反平台分類應由資料配置的原則。該類別與單元測試已刪除，Java 與 Vue 不再包含固定分類對照。

新增 Tenant 級 `fm_process_category` 分類主檔，儲存 code、label、Bootstrap icon、sort order 與 status；`fm_process_def.CATEGORY` 對應分類 code，並新增 `PROCESS_SORT_ORDER` 作為分類內流程排序。Runtime Catalog 預載當前 Tenant 的 ACTIVE 分類後組合回傳；缺少或停用分類時不再 fallback。

流程設計器的分類由自由文字改為資料庫下拉選擇，並新增內建分類管理，可維護名稱、圖示、排序與停用狀態。後端會拒絕停用仍被未停用流程引用的分類。Migration `FM-process-category-metadata.sql` 會為現有流程建立分類資料，並將 A01 的 `PURCHASE` 作為資料 seed，不是程式常數。

本次 Java compile 通過；Runtime Controller、Multi-instance 回歸及新分類 Mapper XML 共 11 項測試通過；包含分類管理 UI 的 Nuxt production build 通過；`git diff --check` 通過。`FM-process-category-metadata.sql` 已於 2026-08-22 套用至本機 `flowmint` MariaDB：`fm_process_category` 已建立且具有 Tenant／code 唯一索引、ACTIVE／INACTIVE 約束與啟用排序索引；`fm_process_def.PROCESS_SORT_ORDER` 為 NOT NULL DEFAULT 0；A01 已有 `PURCHASE／採購／cart／30／ACTIVE`，`FM_PURCHASE_APPROVAL` 的流程排序為 10。回查所有已發布流程缺少 ACTIVE 分類的筆數為 0。

後續 Review 發現新增 `FmProcessCategoryMapper` 第一版雖可編譯與通過 MyBatis parser，但未先完整依 `16-QIFU4後端持久層規範.md` 及 `C:\home\qifu4` core Mapper 實作比對，存在 Java header 缺漏、XML 單行壓縮、insert／update 未使用標準 dynamic trim／set，以及寫入參數缺 `jdbcType` 等不合規問題。現已直接依 QIFU4 `TbSysTemplateMapper` 的 Java 與 XML 八區塊骨架重寫，並重跑 Java compile、Mapper XML parse 與相關回歸測試共 11 項，全數通過。

### 2026-08-22 集團／法人／據點核心邊界澄清

文件已將集團、法人與據點由容易誤解的「暫不排程」改為「核心明確排除」。這些主檔不是 FlowMint 尚未完成的 Backlog，也不納入完成度、Release Gate 或企業簽核能力評分；公司、分公司、廠區與辦公室若只用於簽核路徑，統一使用組織樹節點表達。

若具體業務表單需要簽約主體、統編、付款公司、工安廠區或收貨地址，應使用表單欄位、動態資料服務或 ERP／HR 外部主檔整合，不擴張共用 ORG，也不讓 Resolver 無條件依賴集團、法人或據點資料。HR／ERP 同步、SLA 工作日曆及外部低代碼 Script 則另列為需求驅動的非核心擴充。

### 2026-08-22 申請追蹤、流程進度與附件 Runtime 修正

本輪已完成申請單與待辦頁面的流程可視化及目前簽核資訊。申請詳情 API 現在會回傳 `currentApprovals`，包含目前關卡、指派類型、簽核人帳號與員工顯示名稱；畫面預設顯示目前等待中的關卡與簽核人，若 Flowable 尚未解析出實際人員也會呈現待解析狀態。

新增申請流程圖 API 與前端 `bpmn-js` 對話框。流程圖使用該流程實例實際發布版本的 BPMN XML，並依 Flowable 歷程標示已完成與目前進行中的節點；申請詳情與待辦頁面皆可按「查看流程進度」開啟完整流程圖。對話框關閉後再次開啟會保留可用的 viewer DOM，並重新執行 resize／fit viewport，避免第二次開啟空白。流程圖權限不再只限申請人或實際發起人，目前受派人、候選簽核人，以及已有 `fm_task_action` 流程參與紀錄的人員亦可查看；非流程相關人員仍拒絕存取。

附件顯示已統一正式附件資料來源。申請單及待辦讀取時會將附件 ID 補成 Form.io File 需要的檔名、大小與租戶驗證下載 URL，因此側邊附件清單與表單內附件欄位應顯示同一檔案，並避免 `Press to open`、`NaN Bytes`。待辦送出前會將顯示物件還原成正式附件 ID；後端 `FmTaskFieldPolicyValidator` 亦會將字串 ID、`attachmentId` 與附件 URL 正規化後比較，同一附件僅因前端顯示格式改變不再被誤判為修改，真正替換附件仍會依關卡欄位政策拒絕。沒有附件欄位或附件值為空的表單不受影響。

另修正 `useTaskFieldPolicy` 將 Form.io 數值設定（例如 textarea 的 `rows: 3`）當成 iterable 的問題，避免待辦頁面出現 `number 3 is not iterable` JavaScript 錯誤。

本輪自動驗證結果：Java Maven reactor compile 通過；`FmProcessRuntimeControllerContractTest` 9/9 通過；`FmTaskFieldPolicyValidatorTest` 3/3 通過；Nuxt production build 通過；`git diff --check` 通過（僅既有換行格式警告）。

尚待重新啟動前後端後進行瀏覽器端 E2E，不能列為已驗收：以申請實例 `990a5ecc-9e28-11f1-a3b7-902e16ae3e2d`、待辦 `990ecbac-9e28-11f1-a3b7-902e16ae3e2d`，分別使用 admin 與 fm00830 驗證表單內附件顯示／下載、核准送出、流程圖首次及重複開啟，以及申請人與流程參與者的查看權限；另需用新單驗證附件上傳、後續關卡顯示與下載完整鏈路。

### 2026-08-22 平行加簽規劃

新增 `26-平行加簽規劃.md`，釐清流程設計時 `ALL` 正式全員會簽、`SEQUENTIAL` 依序簽核、既有 Runtime 循序加簽，以及新需求「臨時多人平行加簽」的差異。平行加簽中，原簽核人的 Parent Task 暫停核決，同時建立多筆 Flowable subtask；所有加簽人以唯讀表單回覆同意／不同意及必填意見後，才解鎖原 Task，由原簽核人做最後核決。

規劃涵蓋 Task Policy、平行加簽批次與成員資料表、API、Flowable 實作策略、快照／Action／通知、Vue 操作、併發與安全防線，以及多帳號 E2E 驗收案例。目前僅完成規劃文件，尚未修改資料庫、Java Runtime 或 Vue 畫面，不得標示為已實作。

### 2026-08-22 正式會簽配置與實作指南

新增 `27-正式會簽配置與實作指南.md`，完整說明 FlowMint 現有 `ALL` parallel multi-instance 正式會簽的 BPMN 建模、Approval Group、Task Policy、Assignment Rule、Resolver Preview、正式發布、Flowable Runtime、權限、稽核、Incident 與多帳號 E2E。文件明確區分 `CANDIDATE` 任一人承接、`ALL` 全員會簽、`SEQUENTIAL` 依序簽核、循序／平行加簽，避免把不同核決語意混為同一功能。

文件另列「表單選擇會簽人」為尚未實作的 `FORM_ACCOUNT_FIELD` Resolver 擴充方案，包含欄位格式與後端安全防線；目前不可由 Vue 直接把任意帳號塞入 Flowable，也不得標示為既有功能。本次僅新增文件，未修改資料庫、Java Runtime 或 Vue 行為。

### 2026-08-22 流程簽核改派規劃

新增 `28-流程簽核改派規劃.md`，規劃由具專用權限的流程管理員，在 `/operations/processes` 流程明細中精確選擇正常執行中的 active Task，將原簽核人受控改派給同 Tenant 有效員工。此功能與簽核人自行 `TRANSFER`、工作代理及 `OPEN` Assignment Incident 的 Reassign 分離，不依節點 `ALLOW_TRANSFER`，但強制理由、二次確認、Tenant／Task／目標人員驗證及不可變稽核。

第一版規劃支援一般 assignee、未 claim candidate、`ALL` 會簽中的單一 active instance，以及 `SEQUENTIAL` 目前已建立的 active Task；代理中、循序／平行加簽、申請人補件、Incident 及非執行中流程明確拒絕或導向既有專用操作。規劃涵蓋專用 `FLOWMINT_REASSIGN` 權限、active Task 清單、Preview、options／reassign API、併發與 idempotency、Assignment Snapshot、表單快照、`ADMIN_REASSIGN` Action、通知、SLA、Vue 對話框及多帳號 E2E。目前僅完成規劃文件，尚未修改資料庫、Java Runtime 或 Vue 畫面。

UI 與程式落點已補充至檔案層級：第一版掛載於既有 `FM_PROG008D0001Q`／`/operations/processes` 流程明細，不新增重複選單或 route；Vue 拆至 `components/flowmint/operations` 與 `useTaskReassignmentOperations.ts`，後端另建專責 Task Reassignment Controller／Logic／Validator，避免混入 Incident Operations。查看流程與執行改派維持不同權限，無 `FLOWMINT_REASSIGN` 者只能查看。

### 2026-08-24 平行加簽 Phase A 第一階段

已開始依 `26-平行加簽規劃.md` 實作。發布版 Task Policy 契約新增
`ALLOW_PARALLEL_ADD_SIGN`、`PARALLEL_ADD_SIGN_MAX_MEMBERS` 與
`PARALLEL_ADD_SIGN_COMMENT_REQUIRED`，並完成 Entity、Mapper、Command／View、
版本複製、保存驗證及流程設計器設定。人數上限固定驗證為 1～20，預設為 10，
平行加簽預設關閉；既有 `ALLOW_ADD_SIGN` 在畫面改稱「循序加簽」，避免兩者混淆。

已新增 `FM-parallel-add-sign-schema.sql`，內容包含 Task Policy migration，以及
`fm_task_parallel_add_sign` 批次表與 `fm_task_parallel_add_sign_member` 成員表的
DDL、狀態檢查、計數檢查、Tenant 索引及唯一約束；`flowmint.sql` 的 Task Policy
結構也已同步。本階段未對 MariaDB 執行 DDL 或資料異動。

驗證結果：Maven reactor compile、Task Policy Mapper XML 與發布驗證共 5 項測試、
Nuxt production build 均通過。批次／成員 Entity、Mapper、Service、Runtime API、
Flowable subtask、Action Guard、通知與待辦操作尚待後續切片完成，目前不可標示為
平行加簽已可使用。

持久層骨架已接續新增批次與成員 Entity、QIFU4 `IBaseMapper` Mapper interface、
`IBaseService` Service 及實作。Service 已定義 Tenant-scoped 等待批次、流程批次、
下一批號、成員查詢，以及帶目前狀態／lock version 的完成、計數與取消更新契約。
Maven reactor compile 已通過；MyBatis 八區塊 XML 與 Mapper 契約測試仍待下一切片，
因此本段仍屬進行中，尚不可由 Runtime 呼叫。

批次 Mapper XML 已完成 QIFU4 八個標準 statement，以及等待批次、流程歷程、
下一批號、結果原子遞增、完成與取消專用 SQL。所有狀態異動同時限制 Tenant、
目前 `WAITING` 狀態與 `LOCK_VERSION`；取消另限制尚無成員完成，最後完成則限制
`COMPLETED_COUNT = TOTAL_COUNT`。新增 Mapper XML parser 測試 1 項並通過。
成員 Mapper XML 尚待完成，因此整體持久層仍未結案。

成員 Mapper XML 已完成八個標準 statement、批次成員與待回覆 subtask 查詢，
以及 `PENDING -> AGREED／DISAGREED` 與整批取消的條件式更新。成員回覆同時限制
Tenant、目前狀態與 `LOCK_VERSION`，避免同一 subtask 重複提交。Task Policy、批次、
成員三份 Mapper XML parser 測試共 3 項全數通過。後續仍需將兩張新表同步進完整
`flowmint.sql`、補資料庫結構驗證，再開始 Runtime Logic。

兩張新表已同步納入完整 `flowmint.sql`，包含先刪除成員表再刪除批次表的 FK 安全
重建順序。Migration 與完整建庫腳本已逐項比對兩張表、三個 Task Policy 欄位、
批次／成員唯一鍵及 FK 名稱一致。至此 Phase A 的程式與 DDL 已完成；MariaDB 尚未
套用 migration，下一階段開始建立 Runtime DTO、Logic、API 與 Flowable subtask。

Phase B 已建立 options、start、complete、cancel、detail 的 Command／View 與 Logic
interface 契約。Review 時補上批次 `REQUEST_KEY`，並建立
`TENANT_ID + PARENT_TASK_ID + REQUEST_KEY` 唯一鍵與查詢，確保 API 重送可取得原批次，
而不是建立重複 subtask。Migration、完整 `flowmint.sql`、Entity、Mapper 與 Service 已
同步；兩份 Mapper parser 測試通過。Runtime implementation 與 Controller 尚待接線。

已新增 `FmParallelAddSignRequestValidator`，集中驗證發布版政策開關、1～20 人及節點
上限、重複成員、原處理人、原因與 request key 長度，以及 `AGREE／DISAGREE` 和
必填意見。三項單元測試涵蓋合法成員正規化、停用政策、重複／本人／超額成員、
非法結果及空白意見；連同兩份 Mapper parser 共 5 項全數通過。下一切片開始實際
Runtime 交易與 Flowable subtask 建立。

共用 `IFmRuntimeAuditLogicService` 已新增平行加簽專用稽核方法，在同一 Logic 邊界
建立不可變表單快照與 Task Action，並回傳批次表需要保存的 snapshot OID。Runtime
不需直接操作 Snapshot Mapper／Service，也不會只靠 Flowable local variable 留存稽核。
Maven reactor compile 通過；下一步將此方法接入 start 交易。

已新增平行加簽 start transaction service。服務從 Security Context 與 Header Tenant
驗證操作者，限制原始 active Parent Task、非代理／非 subtask，讀取流程實例固定的
發布版 Task Policy，逐一重驗同 Tenant 有效員工與 membership。交易支援 request key
冪等回傳，建立不可變快照／Action、批次、成員、沿用 Parent due date 的 Flowable
subtask，並設定 Parent waiting local variable；任一步驟失敗會整體回滾。Maven reactor
compile 通過。此服務尚未接 Controller，complete／cancel／detail 尚待完成。

既有 Task Runtime 已加入 Parent waiting 後端守門。平行加簽進行中會拒絕一般
`APPROVE／REJECT／RETURN／RESUBMIT`、Task Transfer、Delegation 與循序加簽；
守門直接讀取 Parent Task local variable，不依賴前端按鈕是否隱藏。Maven reactor
compile 與 `git diff --check` 通過。流程撤回／取消與營運終止如何受控清理 subtask，
仍待 cancel／terminate 整合切片完成。

已完成加簽人 complete transaction service。服務只接受目前 subtask assignee，驗證
Tenant、成員 `PENDING`、批次 `WAITING`、Parent Task 與執行中流程；成員結果與批次
統計分別以 lock version 條件更新。每位成員完成自己的 Flowable subtask 並建立
`PARALLEL_ADD_SIGN_AGREE／DISAGREE` 快照與 Action；最後一人會條件式關閉批次、
解除 Parent waiting 並建立 `PARALLEL_ADD_SIGN_COMPLETE` 稽核。併發衝突整筆交易回滾，
要求呼叫端重新整理後重送。Maven reactor compile 通過；Controller 尚待接線。

已完成整批 cancel transaction service。只有批次發起人可在
`COMPLETED_COUNT = 0` 時取消；服務重新驗證 Parent assignee、執行中流程與 Tenant，
刪除仍存在的 Flowable subtasks，將全部 `PENDING` 成員轉為 `CANCELLED`，再以
`WAITING + LOCK_VERSION + COMPLETED_COUNT = 0` 條件取消批次。成功後解除 Parent
waiting 並建立 `PARALLEL_ADD_SIGN_CANCEL` 快照與 Action；成員數或狀態發生競態時
整筆交易回滾。Maven reactor compile 通過；detail/options 與 Controller 尚待接線。

統一 `FmParallelAddSignRuntimeLogicServiceImpl` 已完成，正式實作 options、start、
complete、cancel 與 detail Logic interface。Options 只回傳同 Tenant 有效員工與有效
membership；detail 只允許批次發起人或批次成員查看，回傳批次統計、可取消狀態及
逐人成果／意見。Start、complete、cancel 分別委派既有交易服務，Controller 不需要
直接操作 Flowable 或單表 Service。Maven compile 與 `git diff --check` 通過；下一步
接正式 Controller endpoint 與契約測試。

五支正式 API 已掛入 `/api/fm/requests/tasks`：options、start、complete、cancel 與
detail。Controller 只委派平行加簽 Logic；Tenant 固定取 `X-FlowMint-Tenant`，Actor
固定取 Security Context，三份 request body 均無 tenant／actor 欄位。Runtime
Controller 契約測試已擴充至 10 項並全數通過，確認端點集合及 body 邊界。

我的待辦已加入平行加簽 subtask 回查。由於 Flowable standalone subtask 沒有一般
BPMN processInstanceId，Inbox 現在會以 Tenant、subtask ID 查詢 `PENDING` 成員，
再由批次精確回到主流程；只有批次 `WAITING` 且主流程 `RUNNING` 才會顯示。Inbox
View 新增 `parallelAddSignTask` 標記供前端顯示「會簽」標籤。Maven test-compile
通過；subtask 的正式唯讀 Task Detail 尚待下一切片整合。

平行加簽 subtask 的 Task Detail 與前端專用回覆已完成。後端依成員與批次回查主流程
及原節點的發布版 Form／Field Policy，將一般核准、駁回、退回、轉派、代理與循序
加簽全部關閉，並回傳 `parallelAddSignTask`。前端以唯讀 Form.io 顯示表單，只提供
「同意／不同意」與必填意見，送至專用 complete API，並提示回覆不會直接決定整張
申請。Maven test-compile、Nuxt production build 與 `git diff --check` 通過。

母任務端的平行加簽操作已完成。Task Detail 新增 `allowParallelAddSign` 與目前有效的
`parallelAddSignDetail`；有等待中的批次時，頁面會隱藏一般簽核動作，顯示完成數與
逐人成員狀態。未發起時可複選有效人員、填寫原因並以 request key 冪等發起；尚無
成員完成時可填寫原因取消。後端 Maven test-compile、Nuxt production build 與
`git diff --check` 通過；MariaDB migration 尚未執行。

流程提前結束的平行加簽清理已完成。申請人撤回、實際發起人取消或管理員終止流程時，
會先刪除仍存在的 standalone 加簽子任務，再以條件更新將所有 `PENDING` 成員及
`WAITING` 批次改為取消；已完成的成員與批次保留稽核事實。清理與原流程終止共用
同一交易，遇到並行更新衝突會整筆回滾。Lifecycle 單元測試 3 項及 Maven
test-compile 通過；MariaDB migration 仍未執行。

平行加簽通知已接入既有通知與郵件 outbox。發起時每位成員收到 `TASK_ASSIGNED`；
每位成員回覆時原簽核人收到 `PARALLEL_ADD_SIGN_REPLIED`；最後一人完成時收到
`PARALLEL_ADD_SIGN_COMPLETED`；整批取消時所有尚待處理成員收到
`PARALLEL_ADD_SIGN_CANCELLED`。通知 ID 由 Tenant、事件、參考 Task 與收件人穩定
產生，可避免重試重複通知，並提供內建中文 fallback 範本文字。通知與 Lifecycle
相關測試共 11 項通過；MariaDB migration 仍未執行。

平行加簽附件授權與管理員受控改派已完成。Standalone subtask 會透過成員與批次回查
母流程及原節點 Field Policy，附件清單與下載均要求同 Tenant、`PENDING` 成員及
`WAITING` 批次。營運管理員可呼叫 `/api/fm/operations/parallel-add-sign/reassign`，
將尚未回覆的子任務條件式改派給同 Tenant 有效員工；禁止改派給目前人員或批次內
既有成員，保留 `ORIGINAL_MEMBER_ACCOUNT`，並寫入稽核與新待辦通知。新增子任務亦
明確設定 Flowable Tenant ID。相關 Controller、Mapper 與附件測試共 9 項通過。

平行加簽 SLA 通知已接入既有 Deadline Job。子任務沿用母任務 due date；排程讀不到
standalone task 的流程變數時，會回退讀母任務的
Tenant、流程定義及發布版本變數，因此既有 `TASK_DUE_SOON`／`TASK_OVERDUE`
通知、穩定通知 ID 與重複執行防重機制可直接套用；原節點 key 則由等待中的批次取得。
Standalone 平行加簽逾期通知單元測試已通過。

2026-08-24 已取得使用者明確同意並對 `flowmint` MariaDB 執行
`FM-parallel-add-sign-schema.sql`。`fm_task_policy` 三個政策欄位、
`fm_task_parallel_add_sign`、`fm_task_parallel_add_sign_member`、所有主鍵／唯一鍵／
查詢索引／外鍵／CHECK constraints 均經 `information_schema` 唯讀查詢確認建立成功。
執行前兩張新表不存在，migration 無錯誤完成。

平行加簽程式完整性複查後補齊完成結果與周邊整合。母任務會回傳最近一批（包含
`COMPLETED`／`CANCELLED`）及逐人成果；申請追蹤與營運流程監控新增所有批次摘要。
Task 頁補上搜尋、已選人員、上限提示、暫停核決確認，以及等待／完成統計與期限；
完成後恢復原簽核動作。指派改用 `PARALLEL_ADD_SIGN_ASSIGNED`，取消通知涵蓋原簽核人、
原始成員及改派後成員。上述變更不需要新增 MariaDB schema。

## 2026-08-24 流程簽核轉派／改派完成

已完成正常 active Task 管理員改派與平行加簽改派的營運畫面。流程監控明細會逐張顯示 active Task、assignee、assignment mode、due date、可改派狀態與阻擋原因；一般 Task 及平行加簽的 `PENDING` member 均可從同一畫面開啟改派操作。

新增 `/api/fm/operations/tasks/reassign-options` 與 `/api/fm/operations/tasks/reassign`。後端固定從 Security Context 取得操作者，並重新驗證 Tenant、RUNNING 流程、active Flowable Task、有效員工與 membership、自簽政策及 `ALL` 會簽重複人員。Incident、代理中、循序／平行加簽中的母 Task 與申請人補件 Task 會明確阻擋或導向專用操作。成功改派保留 Task ID、execution 與 due date，清除舊 candidates、設定新 assignee，寫入 `ADMIN_REASSIGN` Assignment Snapshot、不可變表單快照、Task Action 與新簽核人通知，並使用 Task-local request key 防止相同請求重複寫入。

權限接受系統管理員、`FLOWMINT_REASSIGN` 或相容現有部署的 `FLOWMINT_OPERATIONS`；未新增 MariaDB 資料表或欄位。後端相關 14 項測試、Java compile 與 Nuxt production build 通過。

後續補齊 `/api/fm/operations/tasks/reassign-preview` 與前端預覽／二次確認，Preview 會重新驗證 active Task、Tenant、目標員工、自簽政策與 `ALL` 會簽重複人員。流程監控明細回傳 `canReassign` capability，無權限者不顯示一般或平行加簽改派入口。Idempotency 已增加 payload fingerprint，相同 request key 若更換 target 或 reason 會被拒絕；改派完成後會分別通知原簽核人與新簽核人。

目前轉派／改派程式範圍已完成，不需要新增 MariaDB schema 或資料異動。後端 19 項針對性測試及完整 `org.qifu.fm` 測試 138 項全數通過，Nuxt production build 與 `git diff --check` 均通過；剩餘工作僅為部署環境的多人操作 UAT，不屬於待開發程式。

## 2026-08-25 簽核層級匹配模式

`TARGET_LEVEL_HEAD` 與 `APPROVAL_AUTHORITY` 的 `APPROVAL_LEVEL` 目標已支援三種
層級匹配模式。`EXACT` 僅接受完全相同的簽核層級；`EXACT_OR_HIGHER` 沿直屬主管鏈
選取第一位等於或高於目標層級的人員；`UP_TO_LEVEL` 從直屬主管開始依序收集簽核人，
直到第一位等於或高於目標層級。舊版未保存模式的設定一律相容為 `EXACT`。

流程設計器的指定層級主管及內嵌核決權限規則均已加入模式選擇。`UP_TO_LEVEL` 回傳
有序候選人，直接指定層級的 User Task 發布時必須使用 `SEQUENTIAL` 派送，否則阻擋
發布。主管鏈中沒有符合層級、鏈結循環、超過 `MAX_RESULTS` 或找不到有效人員時均安全
失敗，不會任意從全 Tenant 選取高階主管。後端 compile、設定驗證測試與 Nuxt
production build 已通過；仍需以實際多層主管資料完成三種模式的 MariaDB／Flowable E2E。

### 2026-08-25 請購 Version 2 Level 核決草稿

MariaDB 已建立 `A01／FM_PURCHASE_APPROVAL Version 2` 草稿，Version 1 保持發布且未被
改寫。草稿保留所有案件必經的財務審查，移除總經理／董事長 Gateway 與固定帳號 Task，
改為財務後接單一 `amountApproval`。此 Task 綁定請購 Form Version 1 唯讀畫面，使用
`APPROVAL_AUTHORITY + APPROVAL_LEVEL + UP_TO_LEVEL + SEQUENTIAL`，五段門檻依序為
5 萬、10 萬、20 萬及 30 萬，目標為 L5 經理、L4 處長、L3 副總、L2 總經理及 L1
董事層級。XML、SHA、Task、Form 綁定與五條規則已唯讀回查；Version 2 尚未呼叫 Java
發布服務，因此尚未建立 Flowable deployment，也未退休 Version 1。

### 2026-08-26 請購 Version 2 發布狀態查核

前一節最後所載「尚未發布」是 2026-08-25 建立草稿時的歷史狀態。依本機
`flowmint` MariaDB 唯讀查核，`A01／FM_PURCHASE_APPROVAL` Current Version 已為 2，
流程主檔與 Version 2 均為 `PUBLISHED`，Version 1 已為 `RETIRED`。Version 2 的
Flowable Deployment ID 為 `c13415af-a080-11f1-9f6f-902e16ae3e2d`，Process Definition
ID 為 `FM_PURCHASE_APPROVAL:2:c1a0ba32-a080-11f1-9f6f-902e16ae3e2d`，發布時間為
2026-08-25 20:30:29。後續工作是 Version 2 真實送單、多層主管逐級核決與多人 UAT，
不得再將 Version 2 標示為未部署。

### 2026-08-26 FORM_ACCOUNT_FIELD Resolver

已新增受控的表單選人 Resolver。流程設計器可在 User Task 綁定的已發布表單中選擇
帳號欄位，規則保存為 `{"fieldKey":"..."}`。發布時後端會確認欄位存在於該 Task
綁定的表單版本；Resolver Preview 與正式 Runtime 從 `flowmintFormData` 讀取單一帳號
或帳號陣列，重新驗證同 Tenant 啟用員工、去重並遵守 `MAX_RESULTS`，不接受 Vue
直接指定 Flowable assignee。Assignment Snapshot 與 Multi-instance 沿用既有正式鏈路。

本功能不需新增資料表或 migration。Maven reactor compile、Resolver Config 單元測試
3 項、Nuxt production build 與 `git diff --check` 已通過；尚待以實際表單欄位完成
Resolver Preview 與多帳號 Flowable E2E。

### 2026-08-26 Data Action SELECT Step 重試政策

已完成 Step 級 transient retry 的 Entity、Command／View、Mapper、儲存驗證、Executor
與流程設計畫面。第一版只允許 `QUERY` Action 的 `ONCE SELECT_ONE／SELECT_LIST`，
只捕捉 Spring `TransientDataAccessException`／`RecoverableDataAccessException`；異動 SQL、
Batch、業務驗證與未知錯誤不會重試。每個 Step 可設定 0～5 次及 0～5000ms 間隔，
等待被中斷時會保留 thread interrupt 並停止執行。

Maven compile、SQL Validator 6 項測試、Mapper XML parser 與 Nuxt production build 已
通過。取得使用者明確同意後，已對本機 `flowmint.fm_data_action_step` 執行 migration；
`RETRY_COUNT`、`RETRY_DELAY_MILLIS` 與兩個 CHECK constraints 均回查成功，既有 26 筆
資料全部維持預設值 0，未啟用任何重試。完整結構已同步至 `flowmint.sql`，一次性
migration 依要求於驗證完成後刪除。

### 2026-08-26 Data Action Streaming 第一版

已新增 `POST /api/fm/data-actions/{actionCode}/stream` NDJSON 串流端點，查詢結果會逐列序列化並立即輸出，不再先建立完整 `List`。第一版只允許已發布的 `QUERY` Action，且必須恰好一個啟用中的 `ONCE / SELECT_LIST` Step；不允許 Mutation、FOR_EACH、多 Step 或 Retry，以避免串流已送出後重跑 SQL 造成資料重複。端點沿用 Tenant membership、Request Schema 參數解析、SQL 驗證、個別 Action Rate Limit、Query Timeout、Max Rows 與 Execution Audit；Client 中斷連線或序列化失敗會停止查詢路徑並留下失敗稽核。回應 Content-Type 為 `application/x-ndjson`，每列一個 lowerCamelCase 欄名的 JSON Object，並設定 `Cache-Control: no-store`。

### 2026-08-26 QIFU4 Email 永久失敗同步

在 app 專案以完全相同 FQCN `org.qifu.core.scheduled.SendMailHelperJob` 建立 FlowMint 替代版本，刻意由 Spring Boot `BOOT-INF/classes` 優先取代 core JAR 同名類別；類別 Javadoc 已明確記錄取代關係、禁止改名及 QIFU4 升版比較要求，並以測試驗證 runtime class 來源為 app。未修改 QIFU4 core 原始碼或 `tb_sys_mail_helper` schema。

FlowMint Email 使用既有 `fm_notification.RETRY_COUNT / NEXT_RETRY_DATE / LAST_ERROR / DELIVERY_STATUS` 保存跨排程狀態，每次實際寄送失敗立即以 Tenant、Notification ID、Provider Mail ID 與 `PENDING` 條件累加；總計第三次失敗後轉為 `FAILED` 並停止寄送，成功則同步 `SENT`。app Job 的取件 SQL會在取得前 100 筆時排除已永久失敗及尚未到重試時間的 FlowMint Mail ID，避免失敗資料占滿批次造成後續郵件飢餓；非 FlowMint QIFU4 郵件維持既有單輪三次嘗試行為。

### 2026-08-26 Assignment Resolver 全面複核：已知缺陷，尚未修正

已檢視 `FIXED_ACCOUNT`、`APPROVAL_GROUP`、`INITIATOR_ORG_HEAD`、`PARENT_ORG_HEAD`、`NEXT_HIGHER_LEVEL_HEAD`、`TARGET_LEVEL_HEAD`、`LEVEL_HEAD_CHAIN`、`ROOT_ORG_HEAD`、`DIRECT_MANAGER`、`MANAGER_CHAIN`、`ORG_TITLE`、`ORG_DUTY`、`APPROVAL_AUTHORITY` 與 `FORM_ACCOUNT_FIELD` 全部 Resolver，以及 Config Validator、Preview、發布驗證、Flowable Task Listener、Multi-instance 與 Incident Retry 整合。組織鏈、主管鏈、Approval Level 順序、有效期間、循環偵測及 `MAX_RESULTS` 主體邏輯大致合理，但下列問題屬已確認的 Code 缺陷，修正前不得宣稱 Resolver 全部完成：

1. 正常 Flowable 指派沒有套用 Task Policy 的 `SELF_APPROVAL_POLICY` 與 `DUPLICATE_POLICY`。`ALLOW`、`SKIP_TO_NEXT`、`REQUIRE_ALTERNATE`、`INCIDENT`、`KEEP_EACH_LEVEL`、`MERGE_CONSECUTIVE`、`SKIP_ALREADY_APPROVED` 雖可保存與發布，Runtime 並未完整執行。現行已發布請購 Version 2 使用 `SKIP_TO_NEXT`，`amountApproval` 使用 `SKIP_ALREADY_APPROVED`，因此屬現行流程風險。
2. Resolver 候選人只驗證同 Tenant 的有效 Employee，沒有統一驗證 `fm_tenant_account` 有效 membership，可能派給無法進入該 Tenant 的帳號。2026-08-26 MariaDB 唯讀查核未發現現行有效 Employee、Approval Group Member 或 Org Head 缺少 membership，但程式防線仍不完整。
3. `FORM_ACCOUNT_FIELD` 發布檢查可接受遞迴找到的巢狀 Form.io 欄位，Runtime 卻只用最上層 `form.get(fieldKey)` 取值；Container 內欄位會發布成功但執行找不到。Select 回傳 `{value,label}` Object 時也會把整個 Object 轉成字串，而非取得 Account value。
4. `fallbackConfig` 目前只有 Entity、DTO、保存及 JSON 格式驗證，Resolver Runtime 沒有執行 fallback，屬於表面可設定但實際無效的預留能力。
5. 發布驗證未完整確認 Fixed Account、Approval Group、Org Title、Org Duty、Approval Level、Approval Authority及 Authority Target Ref 的存在、有效期間與 membership；錯誤設定可能發布成功後才在 Runtime 形成 Assignment Incident。`ORG_TITLE` Runtime 亦未先確認 Title 主檔仍啟用且有效。
6. Resolver 在類型分派前一律要求發起人具有 Primary Org Assignment，導致本來不依賴組織歸屬的 `FIXED_ACCOUNT`、`APPROVAL_GROUP`、`FORM_ACCOUNT_FIELD` 也可能不必要地失敗。

相關既有測試 13 項通過，但目前沒有 `FmAssignmentResolverService` 各 Resolver 的直接單元測試，也缺少自簽、重複簽核、membership、巢狀表單帳號及 fallback 回歸測試。本次僅完成 Code Review、既有測試與 MariaDB 唯讀查核，尚未修改 Resolver Code 或資料庫；後續修正優先順序為自簽／重複政策、membership、`FORM_ACCOUNT_FIELD`、fallback、發布引用驗證及解除不必要的 Primary Assignment 前置條件。

### 2026-08-27 Assignment Resolver 缺陷修正

昨日列出的六項缺陷已完成程式修正：Task Listener 會統一套用自簽與重複簽核政策；Resolver 候選人會檢查有效 Tenant membership；`FORM_ACCOUNT_FIELD` 支援巢狀、dot path 與 `{value,label}`；Runtime 會在主 Resolver 未解析時執行 fallback；發布會驗證 Resolver 主檔、Fixed Account membership 與 Approval Authority Target Ref；不依賴組織的 Resolver 不再強制申請人具有 Primary Org Assignment。空 `{}` fallback 維持舊資料相容，視為未設定。

已以乾淨重編譯及指定測試驗證，`FmAssignmentRuleConfigValidatorTest` 與 `FmProcessMultiInstanceBpmnTest` 共 4 項通過，`git diff --check` 通過。完整 Maven test 仍會被專案原有 `MqttIntegrationTest` 缺少 `MqttConfigProperties` 的環境問題阻擋，與本次 Resolver 修正無關。

### 2026-08-27 表單設計與 Runtime 全面複核：已確認缺陷，尚未修正

已對 `FM_PROG005D0001` 表單設計器、草稿與發布版本、Form.io Schema、Data Action Binding、客製 JavaScript，以及起單、簽核、唯讀快照、欄位政策與附件 Runtime 進行 Code Review 與現有測試。下列 3 項有明確可到達的程式路徑與固定結果，屬已確認 Code bug，不是推測、單純缺測或 UAT 風險：

1. 表單發布後端同時接受舊 JSON Schema 與 Form.io Schema，但正式 Runtime `FmFormSubmissionValidator` 只接受根節點含 `components` 陣列的 Form.io Schema。因此以 API 儲存及發布的合法舊 JSON Schema 會發布成功，但送出時必定被 Runtime 拒絕，發布契約與 Runtime 契約不一致。
2. `FmFormSubmissionValidator` 對 `datagrid`／`editgrid` 只在 row 是 `Map` 時驗證內部欄位；非空陣列內若放入字串、數值或其他非物件值，會通過 required 後被直接略過。攻擊者可用非物件 row 繞過明細欄位型別、必填與未宣告欄位驗證。
3. 簽核 Task Runtime 第一次取得表單資料時會以 `serializeAttachmentFields()` 將 Form.io 附件物件轉為 Attachment ID 陣列；執行 `beforeSubmit` 後第二次取得資料卻漏掉相同序列化。核准、駁回、退回或補件重送時，可能將整組 Form.io 附件物件寫入表單資料與快照，而不是規定的 Attachment ID 陣列，導致同一欄位在起單與後續簽核的資料格式漂移。

驗證時後端表單設計、Script、Submission、Field Policy 與 Runtime Controller 相關 34 項測試全部通過，Nuxt production build 亦成功；這些測試未覆蓋上述三條精確路徑，因此不能抵銷已確認的缺陷。本次僅完成驗證與文件登錄，尚未修改程式。

### 2026-08-27 表單設計與 Runtime 缺陷修正

上述 3 項已完成修正：表單發布增加 Runtime Form.io Schema 契約驗證，舊 JSON Schema 必須先經設計器轉換才能發布；Submission Validator 會拒絕非物件 Container 及 `datagrid`／`editgrid` 的非陣列或非物件 row；簽核 Task 在 `beforeSubmit` 後重取資料時會再次將附件欄位序列化為 Attachment ID 陣列。

已新增舊 JSON Schema 發布拒絕、Grid 非物件 row、Grid 非陣列及 Container 非物件的回歸測試。兩個直接驗證類共 20 項通過；擴大執行表單設計、Field Catalog、Script、Submission、Field Policy 與 Runtime Controller 共 37 項亦全部通過，Nuxt production build 成功。
